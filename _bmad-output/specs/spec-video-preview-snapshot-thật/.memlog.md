---
topic: Video-preview thật (AD-22): snapshot JPEG thô qua WS, relay dashboard-backend -> dashboard-frontend, thay placeholder gradient/color-bars ở ChannelGridCell
updated: 2026-09-08T20:55
---

- (decision) Input = code đã implement xong (uncommitted, chưa commit): dashboard-backend/src/core/snapshotRelay.ts, ports SnapshotInboundPort/SnapshotOutboundPort, wsTelemetryAdapter.ts, wsUiAdapter.ts, main.ts + dashboard-frontend uiWsClient.ts/channelStore.ts/ChannelGrid.tsx/ChannelGridCell.tsx+css/page.tsx + toàn bộ test đi kèm, cộng AD-22 (epics.md) + Never-clause đã defer ở spec-2-5. Spec này retroactive hoá 1 tính năng đã code end-to-end nhưng chưa từng có story/spec chính thức trước khi build (vi phạm Ask-First HALT đã bị flag 2 lần ở retro epic 1/2).
- (capability) CAP-1: dashboard-backend nhận event_type=snapshot qua WS Telemetry (transport-core->backend), validate channel_id có mặt + đã đăng ký trong channel-registry + payload.image_base64 là string non-empty, forward NGUYÊN VĂN (channel_id, imageBase64, timestamp) qua SnapshotOutboundPort. Bearer-token/handshake dùng chung cơ chế WS Telemetry hiện có (Story 1.x), không có auth riêng.
- (capability) CAP-2: wsUiAdapter cache khung mới nhất/channel (Map channelId->{imageBase64,timestamp}, ghi đè vô điều kiện không idempotent-guard, mirror lastState của channel-state-change) để replay cho client WS UI connect muộn, đúng 1 lần lúc connect, thứ tự SAU CÙNG (sau registry-snapshot, channel-seen, channel-state-change).
- (capability) CAP-3: dashboard-frontend nhận message channel-snapshot qua WS UI, validate hình dạng thô (channel_id/image_base64 non-empty string), build data-URI (data:image/jpeg;base64,...) DUY NHẤT 1 lần tại thời điểm apply vào channelStore (không phải lúc render), lưu vào channelSnapshots Map, ghi đè theo channelId.
- (capability) CAP-4: ChannelGridCell render ảnh JPEG thật (snapshotDataUri) làm background thumbnail khi effectiveDisplayState là ok/warning (warning vẫn chồng icon cảnh báo lên trên như hiện có); effectiveDisplayState=critical LUÔN dùng color-bars tĩnh hiện có, bỏ qua snapshotDataUri dù có giá trị; thiếu snapshotDataUri (channel chưa nhận khung đầu tiên) fallback gradient placeholder hash-theo-channelId hiện có (Story 2.5), coi là trạng thái 'đang tải ảnh' chứ không phải lỗi.
- (constraint) channel_id KHÔNG có trong channel-registry -> bỏ qua snapshot, log event_type=channel_unregistered, KHÔNG forward qua SnapshotOutboundPort, KHÔNG cache -> chặn 1 channel_id rác/không xác định làm phình cache replay vô thời hạn.
- (constraint) image_base64 forward NGUYÊN VĂN xuyên suốt mọi layer (SnapshotInboundPort, SnapshotRelayService, SnapshotOutboundPort, wsUiAdapter, uiWsClient.ts) - KHÔNG decode/validate/re-encode/diễn giải nội dung ảnh ở backend lẫn frontend transport layer; chỉ channelStore.ts được phép build data-URI (thêm prefix data:image/jpeg;base64,).
- (constraint) KHÔNG debounce snapshot ở bất kỳ layer nào - transport-core tự kiểm soát nhịp gửi (~1.5s/kênh khi CONNECTED, tự ngừng khi RECONNECTING/critical); backend/frontend forward/ghi đè mỗi khung nhận được.
- (constraint) Exception từ implementation của SnapshotInboundPort.handleSnapshot() hoặc SnapshotOutboundPort.publishSnapshot() phải bị bắt (try/catch) và log (snapshot_handler_error / snapshot_publish_error), KHÔNG được thoát ra ngoài làm crash process hay đóng kết nối WS đang có.
- (constraint) Thứ tự replay bắt buộc khi 1 client WS UI connect muộn: registry-snapshot, rồi channel-seen, rồi channel-state-change, rồi channel-snapshot (snapshot LUÔN replay sau cùng) - mỗi loại chỉ replay giá trị MỚI NHẤT/channel, không replay lịch sử.
- (constraint) displayState=critical LUÔN override sang color-bars tĩnh ở ChannelGridCell, bất kể channelSnapshots có data-URI cũ hay không cho channel đó - an toàn hiển thị (tránh ảnh cũ/sai lệch) được ưu tiên hơn continuity hình ảnh.
- (constraint) 'next.config.mjs' allowedDevOrigins thêm IP LAN cố định (192.168.121.21) để dev-server truy cập được từ máy khác trên LAN khi test video-preview - đây là dev-only config, không load-bearing cho production/spec, ghi nhận để không bị hiểu nhầm là quyết định kiến trúc.
- (non-goal) Xử lý/diễn giải nội dung ảnh (resize, re-encode, validate JPEG hợp lệ về mặt byte) ở bất kỳ layer nào trong hệ thống - trách nhiệm duy nhất thuộc về trình duyệt khi gán data-URI vào background-image.
- (non-goal) Lưu trữ lịch sử nhiều khung/kênh (không phải ring buffer ảnh) - chỉ giữ ĐÚNG 1 khung mới nhất/kênh phục vụ replay-cho-client-muộn, khác hẳn HistoryPort bitrate ring-buffer 10-15 phút của Epic 3 (AD-14).
- (non-goal) Panel chi tiết kênh / xem ảnh độ phân giải đầy đủ khi click vào 1 kênh - thuộc Epic 3 Story 3.2 (detail panel), chưa xây, ngoài scope spec này.
- (non-goal) Hành vi gửi/backfill snapshot khi kênh đang critical/machine-offline - transport-core tự ngừng gửi khi critical (theo AD-22 gốc), spec này không định nghĩa cơ chế backfill/gửi lại khi phục hồi.
- (assumption) Input hoàn toàn từ code + comment inline đã có (không phỏng vấn user trực tiếp cho spec này) - giả định comment trong code phản ánh đúng quyết định thật đã chốt lúc code, không có version-drift giữa các file.
- (question) Chưa có giới hạn/backpressure nào được kiểm chứng cho message channel-snapshot cỡ lớn ở WS Telemetry (backend nhận) lẫn WS UI (frontend nhận) - deferred-work.md đã ghi nhận rủi ro tương tự ở TelemetryWsClient::send() phía transport-core (gửi) nhưng chưa xác nhận phía nhận. Cần giới hạn kích thước message không?
- (question) Khi kênh phục hồi từ critical về ok/warning, channelSnapshots vẫn giữ khung CŨ từ trước khi vào critical (không bị xoá khi vào critical) - ảnh cũ (có thể vài giây/phút tuổi) sẽ hiện ngay lập tức trước khi khung mới đầu tiên tới. Đây có phải hành vi mong muốn, hay cần xoá/đánh dấu stale khi chuyển sang critical?
- (event) Pass 2 (Preservation) - Wrapper-only content: next.config.mjs's allowedDevOrigins (IP LAN cố định cho dev-server) là dev-tooling, KHÔNG load-bearing cho contract - không đưa vào SPEC.md, chỉ ghi nhận ở memlog (constraint entry #12) để không bị mất dấu.
- (event) Self-validate Pass 1 (Coherence, rules 1-6+8): PASS. 4 capability đều có intent+success; constraints đều bend thiết kế (không có constraint trang trí); 5 non-goals; success signal cụ thể/demo được; CAP-1..4 ID mới, không trùng; prose lean, không hedge/backstory thừa.
- (event) Self-validate Pass 2 (Preservation): PASS với 1 wrapper-only drop đã ghi nhận (next.config.mjs). Toàn bộ claim load-bearing còn lại từ snapshotRelay.ts, SnapshotInboundPort/OutboundPort, wsTelemetryAdapter.ts, wsUiAdapter.ts, main.ts, uiWsClient.ts, channelStore.ts, ChannelGrid.tsx, ChannelGridCell.tsx/css và test đi kèm đã lên SPEC.md (capabilities/constraints/non-goals) hoặc open-questions. SPEC-video-preview-snapshot-thật finalized (create, 4 capabilities, 0 companions).
- (decision) User resolve open-question #1: KHÔNG cần giới hạn/backpressure cho message channel-snapshot cỡ lớn ở tầng nhận (WS Telemetry/WS UI) - chuyển từ open-question thành non-goal dứt khoát.
- (decision) User resolve open-question #2: CẦN xoá/đánh dấu cache snapshot khi channel chuyển sang critical, ở CẢ backend (wsUiAdapter's lastSnapshot Map) LẪN frontend (channelStore's channelSnapshots Map) - tránh hiện ảnh cũ (từ trước khi vào critical) ngay khi phục hồi về ok/warning, trước khi transport-core gửi khung mới đầu tiên (~1.5s gap).
- (capability) CAP-5: Khi 1 channel chuyển displayState sang critical, cache khung-mới-nhất của channel đó bị xoá ở CẢ backend (wsUiAdapter's lastSnapshot, xoá trong cùng chỗ publishStateChange nhận change.displayState==='critical') VÀ frontend (channelStore's channelSnapshots, xoá trong applyChannelDisplayStateChange cùng điều kiện) - đảm bảo khi channel phục hồi về ok/warning, UI fallback đúng gradient placeholder (đang tải ảnh) thay vì hiện nhầm ảnh cũ trước khi có khung mới.
- (constraint) Xoá cache snapshot khi vào critical áp dụng CẢ subType='machine-offline' (vì machine-offline chỉ có hiệu lực khi displayState đã là critical, theo ChannelGrid.tsx hiện có) - không cần điều kiện riêng, chỉ cần check displayState==='critical'.
- (event) Re-derive SPEC.md sau khi user resolve 2 open-question: thêm CAP-5 + 1 constraint, Open Questions section rỗng -> bỏ, non-goal backpressure chuyển thành dứt khoát (bỏ tham chiếu open-question).
