---
scope: Kiến trúc kỹ thuật cho MVP#1 (PRD prd-TranferFiles-2026-08-28): nền tảng truyền dẫn SRT điểm-điểm (libsrt custom + Blackmagic) và dashboard giám sát nội bộ + cảnh báo (FR-1..FR-14).
purpose: build-substrate
altitude: feature
updated: 2026-08-31T23:02
---

- (constraint) [ADOPTED] Transport: 1 kết nối SRT điểm-điểm độc lập/kênh, dựa trên libsrt tự build + custom orchestration (không dùng Haivision SRT gốc/wrapper thương mại). Sự cố kênh A không ảnh hưởng kênh B (FR-1).
- (constraint) [ADOPTED] Capture/playout SDI qua Blackmagic DeckLink Studio 4K ở cả 2 đầu (đài + trung tâm) (FR-4).
- (constraint) [ADOPTED] Mã hoá AES built-in SRT bắt buộc mọi kết nối, passphrase riêng/kênh, reject handshake sai + log/cảnh báo tần suất bất thường; passphrase là lớp phòng thủ DUY NHẤT cho cổng SRT public-facing (không IP whitelist/VPN trong scope phần mềm) (FR-5, §5).
- (constraint) [ADOPTED] ABR: hạ bitrate chủ động khi mạng xấu đi, phát sự kiện cho dashboard đọc (FR-2). Color bars tại output khi mất kết nối hoàn toàn, tự động, không thao tác thủ công (FR-3).
- (constraint) [ADOPTED] Dashboard: event-driven, không polling; cold-load skeleton→dữ liệu thật per-kênh ~1-2s; debounce ≥5s trước khi đổi trạng thái ok/warning/critical; VU meter cập nhật real-time không qua debounce (FR-6,7,9).
- (constraint) [ADOPTED] Cảnh báo 2 mức: warning(bitrate<70%)→Telegram đội trực; critical(mất tín hiệu)→Telegram+Email+âm báo tại chỗ (1 lần/chuyển trạng thái) cho cả đội trực+lãnh đạo. Debounce≥5s, cooldown 60s/kênh/loại, phục hồi luôn gửi ngay không cooldown (FR-10,11).
- (constraint) [ADOPTED] Ack chỉ thêm ack-label, không đổi màu/badge, tự xoá khi phục hồi về ok (không phải khi ack) (FR-12).
- (constraint) [ADOPTED] disconnected toàn cục (event stream chết): banner đỏ full-width mọi layer, số liệu đứng yên không nội suy, tự phục hồi không cần reload (FR-9).
- (constraint) [ADOPTED] Non-goals vĩnh viễn: không self-service đài địa phương, không SRT Connection Bonding/dự phòng đa đường (hạ tầng mạng), không tắt âm báo vĩnh viễn, không auto-đóng cảnh báo trừ phục hồi thật (§6).
- (constraint) [ADOPTED] Rollout 2 giai đoạn: pilot (subset kênh, số lượng=OQ-4 mở) trong 7 ngày kèm benchmark RTT/latency bắt buộc 1-2 ngày đầu, rồi mở rộng 34 kênh sau khi ổn định + hoàn thiện FR-13/14 + RACI passphrase (§7.3).
- (constraint) [ADOPTED] NFR latency end-to-end ≤1s (nominal, ABR không active); thứ tự ưu tiên tối ưu: SRT buffer/ARQ ≥3xRTT > codec encode/decode > network transit > AES overhead. Khi ABR active: latency không còn là gate cứng, ưu tiên continuity (SM-2 vs continuity conflict rule, §4.1).
- (question) OQ-1 Blackmagic driver/OS, OQ-2 codec ABR, OQ-3 ranh giới kỹ thuật phần mềm/hạ tầng mạng, OQ-4 số kênh pilot, OQ-6 cơ chế lưu trữ/luân chuyển passphrase (RACI vận hành đã tách riêng, không phải phần kiến trúc quyết), OQ-7 state machine retry/reconnect orchestration libsrt — tất cả đang chờ bmad-architecture quyết, ghi nhận từ PRD §9/§10.
- (decision) AD-cand: Ranh giới Transport↔Dashboard — transport-core (tại đài + trung tâm) chỉ phát telemetry thô (bitrate/RTT/connection-state) qua event-contract; dashboard-backend là SINGLE SOURCE OF TRUTH cho debounce≥5s/ngưỡng 70%/cooldown 60s/state ok-warning-critical. Prevents: logic ngưỡng/debounce lặp lại và có thể lệch giữa 34 điểm triển khai transport (khó redeploy) và dashboard (1 điểm, dễ chỉnh sau OQ-9). [User confirmed hướng 1/kênh + máy trung tâm đơn, ngày 2026-08-28]
- (decision) AD-cand: Cách ly lỗi tại trung tâm — 1 process độc lập/kênh (34 process, có supervisor giám sát/tự khởi động lại), không phải 1 process đa luồng xử lý cả 34 kênh. Prevents: crash/leak ở kênh A kéo theo mất giám sát/luồng của 33 kênh còn lại (vi phạm FR-1).
- (constraint) [ADOPTED - user 2026-08-28] Trung tâm: 1 máy vật lý duy nhất chạy toàn bộ 34 process nhận (không phải cluster nhiều máy) cho MVP#1.
- (question) Đầu vào tại đài địa phương ngoài card Blackmagic (SDI live) còn có thêm file media — vai trò/điều kiện kích hoạt chưa rõ (nguồn dự phòng khi mất SDI? nguồn thay thế cho một số kênh không có SDI sống? công cụ test/dev?) — cần hỏi lại user trước khi đưa vào AD nguồn vào (input source abstraction).
- (decision) AD-cand: File media chỉ là input-source thay thế cho MỤC ĐÍCH TEST/DEV — kiểm tra pipeline SRT/encode/reconnect trước khi có phần cứng Blackmagic thật, KHÔNG phải fallback production (khác FR-3 color-bars tại output trung tâm) và KHÔNG phải nguồn thay thế song song cho đài không có SDI sống. Kéo theo: transport-core cần 1 Source abstraction (interface) tách biệt capture logic (BlackmagicSource / FileMediaSource) khỏi encode/ABR/SRT pipeline — cho phép test không cần phần cứng. [User confirmed 2026-08-28, option C]
- (version) Blackmagic Desktop Video SDK: hỗ trợ chính thức Windows/macOS/Linux x86_64 (nguồn: blackmagicdesign.com/developer, sdk-doc.blackmagicdesign.com, xác nhận web 2026-08-28). libsrt: bản ổn định mới nhất ~1.5.4/1.5.6 (nguồn: vcpkg release 2026-07-29, xác nhận web 2026-08-28).
- (decision) AD-cand: OQ-1 resolved — OS = Windows cho toàn bộ máy transport-core (34 đài + trung tâm). [User chọn 2026-08-28, khác khuyến nghị Linux của kiến trúc sư — lý do phía user: quen thuộc vận hành/kỹ thuật viên tại đài]. Kéo theo: driver Blackmagic Desktop Video for Windows, supervisor process dùng Windows Service + auto-restart (không phải systemd).
- (decision) AD-cand: Ngôn ngữ transport-core = C++ (khớp trực tiếp libsrt C API + Blackmagic SDK C++ API, không qua lớp FFI trung gian). [User confirmed 2026-08-28]
- (decision) AD-cand: OQ-2 resolved — Codec ABR = H.264 (x264/hardware NVENC-QuickSync-AMF nếu có), ưu tiên tốc độ triển khai 7 ngày và độ trưởng thành hardware-encode hơn hiệu suất nén của HEVC. [User confirmed 2026-08-28, verified web: H.264 phổ biến/trưởng thành nhất cho contribution feed 2026]. HEVC là candidate tối ưu băng thông cho giai đoạn sau (Deferred).
- (decision) AD-cand: Dashboard-backend chạy CHUNG máy trung tâm với 34 process nhận SRT (không tách máy riêng). Giao tiếp transport→dashboard-backend qua IPC nội bộ (loopback/local socket), KHÔNG cần message broker mạng phân tán cho MVP#1. [User confirmed 2026-08-28]
- (decision) AD-cand: Dashboard-backend = Node.js (event-loop tự nhiên cho event-driven, hệ sinh thái WebSocket/Telegram/Email trưởng thành). Frontend = React (quản lý state 34 ô + panel + ack + debounce transitions). [User confirmed 2026-08-28]
- (decision) AD-cand: IPC transport→dashboard-backend = kết nối loopback trực tiếp (mỗi 34 process C++ là client kết nối outbound tới 1 endpoint local WebSocket/TCP JSON-lines trên backend Node.js) — KHÔNG dùng message broker (NATS/ZeroMQ) cho MVP#1, giữ tối thiểu thành phần hạ tầng. [User confirmed 2026-08-28]
- (decision) AD-cand: OQ-6 (kỹ thuật) resolved — passphrase lưu trong file config mã hoá Windows DPAPI. Đài: 1 file/passphrase riêng cho kênh của đài. Trung tâm: 1 file chứa 34 passphrase index theo channel ID. RACI vận hành (sinh/phân phối/thu hồi) vẫn tách riêng, không do kiến trúc quyết. [User confirmed 2026-08-28]
- (event) SUPERSEDED: quyết định 'trung tâm 1 máy vật lý duy nhất' (entry #15) và 'IPC loopback nội bộ transport→backend' (entry #24) — bị đảo ngược sau khi phát hiện gap phần cứng: DeckLink Studio 4K chỉ 1 SDI in+out/card, không thể phục vụ 34 output đồng thời trên 1 máy. [User correction 2026-08-28]
- (decision) AD-cand (revised): Trung tâm = 34 MÁY VẬT LÝ RIÊNG BIỆT, đối xứng 1:1 với 34 máy tại đài (mỗi máy trung tâm nhận đúng 1 kênh, dùng đúng 1 card DeckLink Studio 4K/máy — khớp nguyên văn FR-4, KHÔNG cần đổi sang Quad 2 nữa). Cách ly lỗi FR-1 đạt ở mức MÁY VẬT LÝ (mạnh hơn process isolation đã bàn trước đó). [User confirmed 2026-08-28]
- (decision) AD-cand (revised): Dashboard-backend (Node.js, state/debounce/alerting/WebSocket) chạy trên 1 MÁY RIÊNG (không phải 1 trong 34 máy trung tâm) — tách tải dashboard khỏi tải SRT/SDI, tránh 1 kênh dùng nhiều tài nguyên làm chậm UI của 33 kênh còn lại, tránh backend crash kéo theo mất luôn 1 kênh truyền dẫn. [User confirmed 2026-08-28]
- (decision) AD-cand (revised): Giao tiếp 34 máy trung tâm → dashboard-backend = kết nối WebSocket/TCP trực tiếp qua LAN nội bộ VTCDigital (mỗi máy trung tâm là client outbound tới backend) — vẫn giữ mô hình kết nối trực tiếp đã chốt, không thêm message broker, chỉ khác là qua LAN thay vì loopback. [User confirmed 2026-08-28]
- (constraint) Địa điểm vật lý: 34 máy đài + 34 máy trung tâm (đối xứng theo kênh) + 1 máy dashboard-backend/UI riêng — tổng ~69 máy vật lý cho MVP#1 đầy đủ (chưa tính pilot subset OQ-4).
- (decision) AD-cand: Lịch sử bitrate cho detail-panel (FR-8) = in-memory ring buffer trên dashboard-backend, ~10-15 phút gần nhất/kênh, không dùng time-series DB cho MVP#1. [User confirmed 2026-08-28]
- (decision) AD-cand: OQ-7 resolved — state machine orchestration/kênh: CONNECTING→CONNECTED; mất mạng→RECONNECTING (backoff tăng dần 1s→2s→4s...trần 30s, lặp vô hạn, không give-up, khớp FR-1); sai passphrase→REJECTED (state RIÊNG, backoff riêng chậm hơn 5s→60s trần; ≥5 lần reject/5 phút→cảnh báo vận hành 'khả nghi dò passphrase'). Hai nhánh backoff độc lập, không dùng chung bộ đếm — tránh nhầm lỗi cấu hình với sự cố mạng. [User confirmed giữ nguyên thiết kế 2026-08-28; ASSUMPTION: các con số backoff/ngưỡng N cụ thể chưa qua benchmark thực tế, cần tinh chỉnh sau khi có dữ liệu pilot]
- (decision) AD-cand: OQ-4 resolved — Giai đoạn thí điểm = 1 KÊNH (1 đài + 1 máy trung tâm tương ứng + 1 máy dashboard-backend). [User confirmed 2026-08-28]
- (question) Tiêu chí 'ổn định' cụ thể để chuyển từ pilot 1 kênh sang mở rộng 34 kênh (thời lượng quan sát không sự cố, SM-2 benchmark đạt ngưỡng nào) — chưa được user xác nhận con số cụ thể, đề xuất mặc định trong Deferred: quan sát liên tục ≥48-72h không sự cố phần mềm ngoài dự kiến + SM-2 benchmark đạt ≤1s nominal, để PM/đội trực xác nhận trước khi mở rộng.
- (decision) AD-cand: Paradigm tổng thể = 'Event-driven actor-per-channel edges + hexagonal central aggregator'. Edge (34 đài + 34 máy trung tâm): mỗi kênh 1 actor độc lập (state machine CONNECTING/CONNECTED/RECONNECTING/REJECTED, không chia sẻ state, khớp FR-1 cách ly lỗi). Center-aggregator (dashboard-backend): hexagonal ports&adapters — lõi debounce/threshold/cooldown thuần nghiệp vụ, adapter ingest (WebSocket/TCP từ 34 máy) + adapter outbound (Telegram/Email/WebSocket→React UI). Toàn hệ thống nối 2 lớp bằng EDA, telemetry 1 chiều actor→aggregator, không polling (khớp FR-7). [User confirmed 2026-08-28]
- (version) Verified web 2026-08-28: Node.js 24 = Active LTS hiện hành (nodejs.org/en/blog/release, endoflife.date/nodejs). React 19.2.8 = bản ổn định mới nhất (react.dev/versions, npm). Blackmagic Desktop Video 16.3 = bản driver/SDK hiện hành cho Windows (blackmagicdesign.com/support).
- (event) Reconcile inputs (4 subagent song song: Brief/PRD/UX/brainstorm) hoàn tất. Tổng hợp gap cần fix: (1) thumbnail/video preview thiếu nguồn dữ liệu trong event-contract, (2) VU meter/audio-level thiếu field telemetry, (3) danh bạ liên hệ đài (tên+SĐT) thiếu registry, (4) Ack thiếu backend port/persistence/identity, (5) FR-6 vị trí lưới cố định thiếu invariant kiến trúc, (6) dashboard-backend thiếu resilience/auto-restart tương đương transport-core, (7) thiếu access-control boundary cho non-self-service (brief), (8) traceability OQ-1/2/4/7 chưa cite rõ trong AD, (9) FR-8 Capability Map thiếu dashboard-frontend, (10) tri-state loading/loaded/no-history-data thiếu invariant ở HistoryPort.
- (decision) Ngưỡng warning = bitrate CÒN LẠI <70% gốc (theo PRD FR-10/Glossary §3, KHÔNG theo cách đọc 'giảm ≥70%' của brief — brief là input kế thừa, PRD final/mới hơn là nguồn thẩm quyền). [User confirmed 2026-08-28, giữ nguyên AD-17]
- (event) Áp fix xong tất cả gap từ reconcile-inputs: thêm AD-22 (thumbnail/snapshot), AD-23 (VU meter/audio_level telemetry), AD-24 (channel-registry: tên đài+đầu mối liên hệ+grid_position), AD-25 (ack-command lên backend, ack-state do backend sở hữu, identity=nhập tay), AD-26 (vị trí lưới cố định là invariant kiến trúc dựa channel-registry), AD-27 (dashboard-backend resilience = Windows Service+auto-restart như AD-3), AD-28 (dashboard chỉ truy cập LAN nội bộ VTCDigital, không self-service cho đài). Sửa AD-14 (tri-state HistoryPort). Gắn traceability OQ-1/OQ-2/OQ-4/OQ-7 vào AD-3/AD-5/AD-19/AD-9. Sửa Capability Map FR-6/FR-8/FR-12. Thêm dòng Deferred traceability cho câu hỏi 'view riêng cho đài' (đã đóng bởi PRD, không phải gap thật).
- (event) Reviewer Gate hoàn tất (rubric + version-verify + adversarial + security + deployment-ops, 5 subagent song song). Áp toàn bộ fix: (1) libsrt pin >=1.5.6 (vá CVE-2026-55868/55869), (2) tách Blackmagic SDK 16.0 vs driver 16.4, Windows->11/Server, (3) sửa AD-1 wording libsrt-vs-appliance, (4) FIX NGHIÊM TRỌNG AD-7: mỗi máy trung tâm chỉ giữ passphrase của chính kênh đó (không index 34), thêm DPAPI scope=machine + note tái cấp qua RACI, (5) AD-9: tách ownership backoff-cục-bộ (actor) vs đếm-ngưỡng-cảnh báo (dashboard-backend, nhất quán AD-11) + mapping severity REJECTED/RECONNECTING, (6) AD-6 thêm entropy passphrase, (7) AD-13 thêm bearer-token/máy cho LAN, (8) AD-22 chốt encoding base64-in-JSON cho snapshot, (9) AD-24 chốt hot-reload + là nguồn liệt kê channel_id chính thức duy nhất, (10) AD-25 sửa envelope khớp convention chung, (11) thêm AD-29 (heartbeat/machine-offline tách biệt disconnected+connection_state) và AD-30 (structured log local, log cả success lẫn reject), (12) AD-20 thêm rule test lõi qua fake port, (13) sửa mâu thuẫn AD-19 vs Deferred RACI (RACI chứng thực trước pilot, lặp lại khi mở rộng — không phải điều kiện còn treo), (14) Consistency Conventions thêm schema_version + enum đóng cho connection_state/event_type, (15) Deferred mở rộng: update/patch 68 máy, rollback/canary, log aggregation, backup channel-registry. Lint lại: 0 findings.
- (event) spine finalized
- (decision) Amend (khong doi AD-ID): nguoi dung xac nhan lai qua phien bmad-party-mode 2026-08-30/31 - tong quy mo du an la 20 dai, KHONG phai 34. Sua Rule tai cho: AD-2 (20 may trung tam doi xung 20 may dai), AD-3 (20 may dai + 20 may trung tam chay Windows), AD-7 (20 kenh, khong phai index 34), AD-9 (nguon la trong 20 kenh), AD-12 (ngoai 40 may transport, khong phai 68), AD-13 (20 may trung tam), AD-15 (khong cho du 20 kenh), AD-19 title/Rule (mo rong 20 kenh, khong phai 34), AD-20 (20+20 may that), AD-24 (thay 34 kenh doc lap thanh 20), AD-30 (log rai rac 41 may, khong phai 69), tech-stack table (40 may transport-core), mermaid diagram (Dai dia phuong x20, Trung tam x20), thu muc dashboard-backend inbound comment (20 may trung tam), state/ store (20 kenh), Capability Map FR-6 (luoi 20 kenh), tat ca dong Deferred (33->19, 34->20, 68->40, 69->41). Da grep xac nhan 0 con lai tham chieu 34/68/69/33. Ly do: PRD da duoc sua truoc (v5, updated 2026-08-31) voi cung ly do.
- (event) spine amended and re-finalized (update: quy mo 34->20 dai). Lint pass: 0 findings. Full reviewer-gate subagent (rubric/adversarial/version-verify) khong chay lai vi day la sua so lieu co hoc, nhat quan, khong phai quyet dinh kien truc moi - da xac minh bang grep toan file 0 con lai tham chieu 34/68/69/33. Con lai CHUA dong bo: brief-TranferFiles-2026-08-27/brief.md va ux-TranferFiles-2026-08-27/DESIGN.md+EXPERIENCE.md van con '34' (bao gom quyet dinh layout luoi 6x6=36 o, 2 o du phong, tuned cho 34) - can bmad-ux/cap nhat brief rieng, da bao cho user.
- (event) Sync-check (forwarded via bmad-create-architecture): grep brief.md + ux DESIGN.md/EXPERIENCE.md/mockups cho '34/33/36' -> 0 con lai. Xac nhan UX memlog (entries #45-47) da tu dong bo 34->20 truoc do (bo cuc luoi 5x4=20, khong du phong), bao gom ca mockups/key-channel-grid.html va color-themes-1.html. Con lai '34' chi trong .memlog.md (lich su, giu nguyen theo dung) va cac review report cu da dong dau thoi diem truoc amend (review-accessibility.md, validation-report.html, review-rubric.md - snapshot Reviewer Gate cu, khong phai spine/design chinh thuc). Khong can amend AD nao - day la doc-sync, khong phai quyet dinh kien truc moi. Khong re-run reviewer gate vi spine khong doi noi dung.
- (decision) AD-4 amended (2026-08-31, sua User confirmed 2026-08-28): transport-core van C++, van 1 tien trinh/kenh, KHONG subprocess/CLI ffmpeg - nhung thay vi tu viet capture(Blackmagic SDK)+encode(x264) tu dau, link truc tiep thu vien FFmpeg (libavformat/libavcodec/libavdevice) vao trong tien trinh C++ nhu mot native C API, cung cach goi nhu libsrt hien tai. Ly do: team hien chua co kinh nghiem Blackmagic SDK, viet capture/encode pipeline tu zero la rui ro trien khai lon (phat hien trong phien party-mode 2026-08-31); libavdevice da wrap san module decklink capture/output da kiem chung, giam dung phan kho nhat ma khong vi pham ly do goc AD-4 (tranh FFI/subprocess trung gian). AD-8 Source abstraction (BlackmagicSource/FileMediaSource) khong doi - BlackmagicSource gio implement bang libavdevice decklink thay vi goi truc tiep Blackmagic SDK.
- (version) FFmpeg (libavformat/libavcodec/libavdevice) pin = nhanh on dinh 8.1.x (8.1.2, verified web 2026-08-31 - nhanh 8.1 la release stable moi nhat, uu tien on dinh production 24/7 hon nhanh moi nhat n9.0.1). Build FFmpeg voi --enable-decklink van can Blackmagic Desktop Video SDK 16.0 (AD giu nguyen, khong doi) tai build-time cua chinh FFmpeg, khong phai code transport-core goi truc tiep SDK nua.
- (decision) AD-31 (moi): Cau truc thu muc transport-core/src/source/ va pipeline/ cap nhat theo AD-4 amended - source/ chua BlackmagicSource (wrap avdevice decklink input/output qua libavformat), pipeline/ dung libavcodec encode H.264 (van giu AD-5 x264/hardware NVENC-QuickSync-AMF qua libavcodec) + ABR loop tu viet doc srt_bstats qua libsrt (khong doi, AD-10/AD-11 khong doi). Binds: AD-4, AD-5, AD-8, AD-10. Prevents: nham lan giua 'dung thu vien FFmpeg' (giu nguyen tinh than AD-4, khong FFI/subprocess) voi 'goi CLI ffmpeg' (vi pham AD-4).
- (decision) AD-32 (moi, tu adversarial review 2026-08-31): Dinh dang payload qua ket noi SRT = MPEG-TS, mux/demux bang libavformat (custom AVIOContext ghi/doc qua libsrt truc tiep, khong qua network layer cua avformat - giu dung AD-31: libsrt van goi truc tiep de ABR doc srt_bstats). Ly do: quy uoc chuan cong nghiep cho SRT (Haivision khuyen dung), tan dung muxer/demuxer da co san cua libavformat thay vi tu viet framing/PTS-DTS tu dau - dung tinh than giam rui ro code-tu-viet cua AD-4 amended. Dong xoa go tim ho trong critical cua adversarial review: nhanh trung tam (srt-recv->demux MPEG-TS->decode->playout) gio co dinh nghia ro, khong con la khoang trong khong AD nao chi phoi.
- (decision) AD-8 sua (2026-08-31): tach ro Source (dai, capture qua libavdevice decklink input) va Sink moi ten PlayoutSink (trung tam, output qua libavdevice decklink output) - truoc do AD-31 viet nham BlackmagicSource lam ca input/output, mau thuan voi AD-8 goc chi dinh nghia Source la capture. Sua AD-31 khop lai, khong con mau thuan.
- (constraint) AD-4 amended bo sung (2026-08-31, tu adversarial review): (a) libav* link STATIC vao 1 exe/may - khong dynamic-link nhieu DLL roi rac, tranh DLL-hell/ABI-drift qua 40 may; (b) build FFmpeg voi --disable-libsrt (khong dung libsrt noi tai cua FFmpeg) de tranh vendor 1 ban libsrt thu 2 khong dam bao pin >=1.5.7 - app chi dung dung 1 ban libsrt da pin truc tiep; (c) build flags --enable-gpl --enable-nonfree --enable-libx264 --enable-decklink (+ --enable-nvenc/--enable-amf tuy may) PHAI pin co dinh trong script build dung chung, khong de tuy y tung may tu build khac flags.
- (constraint) Sua loi trong Stack table (phat hien qua reviewer gate 2026-08-31, khong lien quan AD-4): libsrt CVE so sai (55840/55841 -> dung la CVE-2026-55868/55869, memlog entry cu #o ghi dung, spine transcribe sai) va version loi thoi (1.5.6 -> Haivision phat hanh 1.5.7 ngay 28/08/2026, verified web 2026-08-31). Capability Map thieu dong AD-4/AD-8 cho FR-1/2/4/5. Blackmagic SDK/driver pin lai thanh 1 cap co dinh (16.0/16.4) xuyen suot rollout AD-19, khong con ghi 'hien hanh tai thoi diem cai dat' de tranh ABI-drift giua may pilot va may mo rong sau.
- (question) Open item (khong fix trong lan update nay, ngoai pham vi AD-4): rubric-walker reviewer flag CVE-2025-55182 (RCE, CVSS 10.0) tren React - can nguoi dung tu xac minh version/applicability cho dashboard-frontend (SPA thuan, chua ro co bi anh huong khong). Revisit truoc khi trien khai production, khong block AD-4 update nay.
- (event) Spine updated & finalized lai (2026-08-31): AD-4 amended (FFmpeg libav* link truc tiep, khong subprocess), AD-8 amended (Source/Sink tach roi), AD-31 + AD-32 moi (implementation encode/decode + payload MPEG-TS qua libavformat). Reviewer gate (rubric-walker, version-verify, adversarial) da chay va tat ca finding da duoc fix hoac ghi Deferred (React CVE-2025-55182, ngoai pham vi). Lint sach (0 findings).
