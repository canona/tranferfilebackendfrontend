---
epic: 2
date: 2026-09-07
verdict: accepted-with-open-items
criteria: declared
headless: false
---

# Epic 2 Retrospective — Giám sát trạng thái kênh thời gian thực & phân loại cảnh báo trên lưới

## Epic summary

- **Epic:** 2 — dashboard-backend + dashboard-frontend, 7 stories (2.1 → 2.7), tất cả `done`. `pending_stories`: rỗng (xác nhận qua `sprint_status.py detect-epic --epic 2`).
- **Đi vào retro:** anh CaoNN không có concern cụ thể nào cần ưu tiên riêng — chạy retro theo evidence chuẩn.
- **Phạm vi VCS:** Epic 2 nằm trong 1 git repo riêng (`TranferFiles`, root), tách biệt hoàn toàn khỏi `transport-core/` (Epic 1, git repo riêng của nó — `NO_VCS`/`baseline_commit` khác trong các story file chỉ là do dùng chung mẫu spec). Commit đầu tiên của repo này (`52f312f`) CHÍNH LÀ commit đầu của Epic 2, không có lịch sử trước đó.
- **Diff range:** `4b825dc642cb6eb9a060e54bf8d69288fbee4904..7412cd2` (empty-tree → HEAD, vì `52f312f` là root commit không có parent thật). 9 commit, 64 file thay đổi, 0 merge.
  - Commit → story: `52f312f` (2.1+2.3, squashed — 2.2 không tự nhận diện được qua regex subject vì message chỉ ghi "2.1-2.3", không có `\b2.2\b`, đã tự đối chiếu thủ công qua nội dung diff/story file để xác nhận 2.2 nằm trong cùng commit), `b8b7b43`+`32f71a7` (2.4), `75c49378`+`ca68eea9` (2.5), `870d4fe3`+`40f4e863` (2.6), `b444a5c5`+`7412cd28` (2.7).
  - Top churn: `dashboard-frontend/package-lock.json` (+3066, lockfile), `main.test.ts` (+672/6 commit — composition root, chạm bởi mọi story), `channelState.ts` (+353/-7/3 commit), `uiWsClient.ts` (+286/-33/5 commit) — 2 file cuối là các file "xương sống" chạm bởi nhiều story nhất, khớp đúng với các finding aggregate bên dưới.
- **Evidence có sẵn:** 7 story spec file đầy đủ (Intent/Boundaries/Code Map/Review Findings/Spec Change Log), `epic-2-context.md`, `deferred-work.md` (198 dòng, log defer xuyên suốt 7 story + kế thừa từ Epic 1), `epics.md` (AC cấp epic, đã declared), sprint-status.yaml, retro Epic 1 (`epic-1-retro-2026-09-02.md`). Codebase chạy được (build+test) tại thời điểm retro.
- **Evidence thiếu:** Không có session log/conversation record nào cho từng story (chỉ có story file tổng hợp cuối) — process-lesson analysis dựa hoàn toàn vào nội dung "Design Notes"/"Spec Change Log" tự ghi trong story file, không phải log phiên làm việc thật. Không xác nhận được bằng chứng độc lập ngoài lời tự thuật của story file cho các mục "đã hỏi người dùng" — chấp nhận làm nguồn duy nhất vì đây là hiện trạng chuẩn của quy trình bmad-build trong dự án này (đã áp dụng y hệt ở retro Epic 1).

## Findings

Toàn bộ 7 story đã qua code review nhiều vòng (2-3 vòng/story, 4-layer: Blind Hunter/Edge Case Hunter/Verification Gap/Acceptance Auditor) trước khi retro này chạy — phần lớn finding mức story đã được patch hoặc defer có lý do rõ trong `deferred-work.md`. Các finding dưới đây là finding **aggregate cấp epic** — mẫu hình chỉ lộ ra khi nhìn xuyên suốt nhiều story/review, không phải finding mức 1 story.

### F1 — [fix-now] Mẫu "Map không bao giờ prune" lặp lại độc lập ở 4 nơi, chưa từng được xử lý dù bị gắn cờ 4 lần

`this.channels` (`dashboard-backend/src/core/channelState.ts:79`), `seenChannels` (`wsUiAdapter.ts:124`), `lastState` (`wsUiAdapter.ts:133`), và phía frontend `channelStore.ts`'s state không được reset khi nhận `registry-snapshot` mới — cả 4 chỉ tăng, không bao giờ prune theo vòng đời channel-registry. Xác nhận bằng grep trực tiếp: không có lệnh `.delete(`/prune nào trong `channelState.ts`/`wsUiAdapter.ts`.

Đây LÀ 4 finding riêng biệt trong `deferred-work.md` (Story 2.2 dòng 68, Story 2.3 dòng 77-78/152-153, Story 2.6 dòng 149-153, Story 2.7 dòng 185-186/197-198) — mỗi lần đều được đánh giá "rủi ro thấp ở quy mô ~20 kênh" và defer lại **cùng 1 root cause** mà chưa lần nào được gộp thành 1 quyết định kiến trúc duy nhất ("prune theo registry reload, làm 1 lần cho cả 4 chỗ"). Rủi ro tích luỹ: quy mô dự kiến mở rộng (AD-19), và giả định "sửa registry không thường xuyên" chưa được đối chiếu với tần suất vận hành thật.

- **Nguồn:** [`channelState.ts:79`](dashboard-backend/src/core/channelState.ts#L79), [`wsUiAdapter.ts:124,133`](dashboard-backend/src/adapters/outbound/wsUiAdapter.ts#L124), `deferred-work.md:68,77-78,149-153,185-186,197-198`.
- **Kịch bản lỗi cụ thể:** Admin xoá 1 kênh khỏi `channel-registry.json` (hot-reload) rồi thêm lại 1 kênh khác vào đúng `grid_position` đó sau vài tuần vận hành — badge/audit cũ của kênh bị xoá vẫn tồn tại vô hạn trong bộ nhớ tiến trình, không ảnh hưởng UI hiện tại (registry-snapshot mới nhất che khuất) nhưng tích luỹ không giới hạn qua nhiều lần sửa registry trong 1 dịch vụ chạy 24/7 không restart.

### F2 — [fix-now] Vi phạm hướng layering, đang lớn dần: `DisplayState` định nghĩa trong file component UI, bị state/service layer import ngược

`DisplayState` được định nghĩa tại [`ChannelGridCell.tsx:36`](dashboard-frontend/src/components/ChannelGridCell.tsx#L36) (1 file component UI) rồi bị `channelStore.ts`/`uiWsClient.ts` (state/service layer) import ngược từ Story 2.6 trở đi — đã bị defer ở review Story 2.6 (`deferred-work.md:155-156`) là "đào sâu 1 pattern có từ Story 2.4", chưa từng được dời sang 1 module type trung lập (đối xứng với cách backend đã làm cho `AlertOutboundPort.ts`). Xác nhận vẫn còn nguyên trạng sau Story 2.7 (không file mới nào sửa vị trí định nghĩa này).

- **Nguồn:** `ChannelGridCell.tsx:36`, `deferred-work.md:155-156`.
- **Kịch bản lỗi cụ thể:** Không phải bug runtime, nhưng là nợ kiến trúc tăng dần — Epic 3 (detail-panel) hoặc Epic 5 nhiều khả năng cần thêm 1-2 consumer nữa cho `DisplayState`, càng để lâu chi phí dời càng cao (đã lan ra 3 file).

### F3 — [defer] Kích thước lưới (20/5/0-19) hard-code độc lập ở 3 nơi không chia sẻ nguồn chân lý

`GRID_SIZE=20` ([`ChannelGrid.tsx:38`](dashboard-frontend/src/components/ChannelGrid.tsx#L38)), `GRID_COLUMNS=5` ([`ChannelGridCell.tsx:31`](dashboard-frontend/src/components/ChannelGridCell.tsx#L31)), `GRID_POSITION_MIN/MAX=0/19` ([`fileChannelRegistryAdapter.ts:33-34`](dashboard-backend/src/adapters/outbound/fileChannelRegistryAdapter.ts#L33)) — đã bị gắn cờ 2 lần (Story 2.3 vòng 2 và vòng 3, `deferred-work.md:92,119-120`), chưa gộp vì frontend/backend là 2 npm project riêng không share code. Xác nhận vẫn còn nguyên trạng.

- **Nguồn:** như trên + `deferred-work.md:92,119-120`.
- **Disposition:** defer hợp lý (không blocking, roadmap hiện tại không đổi layout 5×4) — nhưng gộp cùng F1/F2 thành 1 action item "dọn nợ kỹ thuật" vì cùng nhóm nguyên nhân (thiếu module dùng chung xuyên 2 project).

### F4 — [fix-now] Không có bằng chứng tích hợp thật giữa transport-core (Epic 1) và dashboard-backend (Epic 2) qua đúng ranh giới 2 epic

`transport-core/src/telemetry/TelemetryWsClient.cpp` (bên gửi, Epic 1) và `dashboard-backend/src/adapters/inbound/wsTelemetryAdapter.ts` (bên nhận, Epic 2) cùng implement 1 hợp đồng envelope (`schema_version`/`event_type`/field names) độc lập bằng 2 ngôn ngữ, 2 repo, 2 phiên làm việc khác nhau — nhưng chưa từng chạy chung. Xác nhận: `dashboard-backend/tests/wsTelemetryAdapter.test.ts:1` chỉ ghi "Mirror test_telemetry_ws_client.cpp" (comment), không gọi mã C++ thật; `transport-core/tests/test_telemetry_ws_client.cpp:4` chỉ ghi "dashboard-backend needed... test qua fake seam". Mỗi bên tự viết mock cho phía kia.

- **Nguồn:** `dashboard-backend/tests/wsTelemetryAdapter.test.ts:1`, `transport-core/tests/test_telemetry_ws_client.cpp:4`.
- **Kịch bản lỗi cụ thể:** Đây đúng là loại ranh giới "không phiên nào từng thấy cả 2 phía" mà Phase 2 của retro được thiết kế để bắt — 1 lệch hợp đồng nhỏ (vd field `audio_level` thứ tự [L,R] vs [R,L], hoặc `bitrate_kbps` đơn vị) sẽ không bị bất kỳ test nào trong 2 repo phát hiện, chỉ lộ ra khi pilot 1 kênh thật chạy.

### F5 — [fix-now, cần điều tra thêm] `dashboard-backend`'s test suite hiện KHÔNG pass 100% trên môi trường/toolchain hiện tại

Chạy thật `npm run build && npm test` tại thời điểm retro (Node v24.18.0, Windows): build sạch, nhưng **108/109 pass, 1 fail** — `tests/installService.test.ts`'s test "regression patch #1" (timeout-reject khi `start` không bao giờ fire) crash với lỗi nội bộ của `node:test` runner: `Error: Unable to deserialize cloned data due to invalid or unsupported version` tại `node:internal/test_runner/runner`. Đã tái hiện 3 lần độc lập (chạy cả suite, chạy riêng file, chạy riêng đúng test qua `--test-name-pattern`) — lỗi nhất quán, không flaky. Các test khác (bao gồm cả `channelState.test.ts` chạy riêng: 24/24 pass) không bị ảnh hưởng.

- **Nguồn:** chạy trực tiếp `cd dashboard-backend && npm test` tại retro; `tests/installService.test.ts:106-116`.
- **Đánh giá:** dấu hiệu (lỗi nằm ở tầng deserialize IPC của chính test-runner, không phải ở assertion) cho thấy nhiều khả năng là vấn đề tương thích `node:test`/Node 24 trên môi trường này hơn là 1 bug logic thật trong `installService.ts` — nhưng đây là hành vi quan sát được THẬT, mâu thuẫn với tuyên bố "verify: 49/49 test pass" của Story 2.1/2.2 nếu chạy lại đúng bằng công cụ/phiên bản Node hiện tại. Cần xác nhận: phiên bản Node dùng lúc code review gốc có khác phiên bản hiện tại không; nếu môi trường CI/deploy thật cũng là Node 24, đây là rủi ro cần xử lý trước khi tin tưởng lại "test xanh" của riêng file này.

### F6 — [process lesson] epic-1-retro-item-7 ("Ask First phải là HALT thật") chỉ landed một phần trong Epic 2

- Story 2.1: Ask-First "channel_id lạ không có baseline" được code trước, chỉ resolve qua code review SAU đó — Design Notes tự thừa nhận "chưa có bằng chứng đã hỏi trước khi code" ([spec-2-1, dòng 98](_bmad-output/implementation-artifacts/spec-2-1-dashboard-backend-ranh-giới-telemetry-thô-trạng-thái-tính-toán.md#L98)).
- Story 2.2: Ask-First "fs.watch atomic-rename" bị defer ở review vòng 1 thay vì HALT ngay, chỉ escalate thành HALT thật ở vòng 2 sau khi 2 layer review độc lập xác nhận lại ([spec-2-2, dòng 108](_bmad-output/implementation-artifacts/spec-2-2-channel-registry-hot-reload-nguồn-liệt-kê-kênh-vị-trí-lưới-chính-thức.md#L108)).
- Story 2.7 (đối chứng tích cực): xung đột subType `machine-offline`/`config-or-security-suspected` được HALT đúng quy trình, ghi ngày, ghi rõ lý do người dùng chấp nhận rủi ro ([spec-2-7, dòng 82](_bmad-output/implementation-artifacts/spec-2-7-xử-lý-mất-kết-nối-dữ-liệu-giám-sát-disconnected-máy-trung-tâm-chết-machine-offline.md#L82)) — đúng khuôn mẫu bài học Epic 1 đã yêu cầu.
- **Đánh giá:** bài học chưa được nội hoá nhất quán — 2/3 trường hợp đầu epic lặp lại đúng anti-pattern Epic 1 đã ghi nhận, trường hợp cuối mới đúng quy trình.

### F7 — [process lesson] epic-1-retro-item-8 ("git status sạch trước khi set done") tái diễn ở cuối Epic 2

Tại thời điểm bắt đầu retro này, `sprint-status.yaml` đã ghi `2-7: done` (`last_updated: 09-07-2026 00:00`) nhưng `git status` cho thấy 3 file (`channelState.ts`, `channelState.test.ts`, `uiWsClient.ts`) — chính là patch review vòng 2 của Story 2.7 — vẫn ở trạng thái uncommitted. Các thay đổi này được commit sau đó trong cùng ngày (`7412cd2`, `2026-09-07T05:18:41+07:00`), tự sửa trước khi retro hoàn tất, khá hơn tình huống Epic 1 (7 patch tồn đọng, chỉ bị chính retro phát hiện) — nhưng cùng gốc: chưa xác nhận git status sạch trước khi set `status: done`.

- **Nguồn:** git status snapshot đầu phiên (trong system-reminder), `git log -1 --format=%cI 7412cd2` = `2026-09-07T05:18:41+07:00`, sprint-status.yaml dòng 32/56.

## Behavior verification

Đã build + chạy test thật (không chỉ đọc log tuyên bố trong story file):

- `dashboard-frontend`: `npm run build` sạch (Next.js 16.3.4/Turbopack), `npm test` — **109/109 pass** (Vitest, 7 file test).
- `dashboard-backend`: `npm run build` sạch (tsc), `npm test` — **108/109 pass** — xem F5.
- Không chạy manual E2E thật (cần 20 máy trung tâm/hardware DeckLink theo ghi chú Epic 1 retro) — hành vi runtime của luồng WS thật (telemetry → debounce → broadcast → frontend re-render) chỉ được xác nhận qua test tích hợp nội bộ từng story (đã đọc, đáng tin), không qua chạy tay `npm run dev` thật trong phiên retro này.

## Previous-retro follow-through (từ `epic-1-retro-2026-09-02.md`)

| id | Trạng thái file | Landed? | Đề xuất |
|---|---|---|---|
| `epic-1-retro-item-1-commit-merge-7-patch-round-2-của-story-1` | done | Đã done từ trước, không cần xét lại | — |
| `epic-1-retro-item-2-sửa-thứ-tự-reset-backoff-trong-channelac` | open | Không có bằng chứng — thuộc `transport-core/` (Epic 1, C++), Epic 2 không chạm file này | Không đề xuất đổi (ngoài phạm vi evidence Epic 2) |
| `epic-1-retro-item-3-bọc-try-catch-quanh-h264decoder-decoder` | open | Không có bằng chứng — thuộc `transport-core/`, ngoài phạm vi Epic 2 | Không đề xuất đổi |
| `epic-1-retro-item-4-verify-guard-xung-đột-thiết-bị-duplex-tr` | open | Không có bằng chứng — thuộc `transport-core/`, cần hardware thật | Không đề xuất đổi |
| `epic-1-retro-item-5-mở-story-mới-forward-frame-decode-thật-v` | open | Không có bằng chứng — thuộc `transport-core/` | Không đề xuất đổi |
| `epic-1-retro-item-6-gộp-3-điểm-blocking-call-không-timeout-s` | open | Không có bằng chứng — thuộc `transport-core/` | Không đề xuất đổi |
| `epic-1-retro-item-7-quy-trình-ask-first-trong-frozen-boundar` | open | **Landed một phần** — xem F6: tái diễn ở Story 2.1/2.2 vòng 1, đúng quy trình ở Story 2.7 | Không đề xuất đổi trạng thái (chưa nhất quán đủ để coi là "done"); có thể cân nhắc `in-progress` nếu anh CaoNN đồng ý ghi nhận tiến bộ ở Story 2.7 |
| `epic-1-retro-item-8-quy-trình-checklist-đóng-story-bmad-buil` | open | **Tái diễn 1 lần** — xem F7: Story 2.7 patch round 2 uncommitted khi đã `done`, tự sửa cùng ngày | Không đề xuất đổi trạng thái |
| `epic-1-retro-item-9-dọn-nhóm-finding-mức-thấp-khi-quay-lại-l` | open | Không có bằng chứng — thuộc `transport-core/` | Không đề xuất đổi |

## Action items

**Fix-now (dev):**
1. **[dev]** Gộp 4 điểm "Map không prune" (F1) thành 1 cơ chế duy nhất: khi channel-registry hot-reload xoá 1 `channel_id`, dọn tương ứng ở `ChannelStateService.channels`, `wsUiAdapter.ts`'s `seenChannels`/`lastState`, và có tín hiệu để frontend tự dọn `channelDisplayStates`/`channelMachineOffline`. Giải quyết 1 lần thay vì tiếp tục defer rời rạc từng nơi.
2. **[dev]** Dời `DisplayState` (F2) sang 1 module type trung lập (`dashboard-frontend/src/types/` hoặc tương đương), cập nhật `channelStore.ts`/`uiWsClient.ts`/`ChannelGridCell.tsx`/`ChannelGrid.tsx` import từ đó thay vì từ component.
3. **[dev]** Gộp hằng số kích thước lưới (F3) — cân nhắc cùng lúc với item 1/2 vì cùng nhóm "nợ kỹ thuật thiếu module dùng chung".
4. **[dev]** Điều tra lỗi `node:test` IPC ở `installService.test.ts` (F5) trước khi tin lại "test xanh" trên toolchain hiện tại — xác nhận phiên bản Node CI/deploy thật, thử cô lập/patch test hoặc pin phiên bản Node.

**Spec reconciliation / cần quyết định (pm/architect):**
5. **[architect]** Lên lịch 1 checkpoint tích hợp thật giữa `TelemetryWsClient` (transport-core) và `wsTelemetryAdapter` (dashboard-backend) trước khi pilot 1 kênh thật chạy (F4) — hiện chỉ có mock 2 phía, chưa có bằng chứng 2 hệ thống nói cùng 1 ngôn ngữ.

**Process lessons (process-owner, tái khẳng định từ Epic 1):**
6. **[process-owner]** Ask-First trong Boundaries là HALT thật trước khi code — Story 2.1/2.2 (vòng 1) của Epic 2 vẫn lặp lại đúng anti-pattern Epic 1 đã ghi nhận (F6); Story 2.7 là ví dụ đúng cần nhân rộng.
7. **[process-owner]** Checklist đóng story cần xác nhận `git status` sạch trước khi set `done` — tái diễn 1 lần ở cuối Epic 2 (F7), dù tự sửa cùng ngày.

## Acceptance verdict

**accepted-with-open-items** (criteria: **declared** — `epics.md`'s Epic 2 section, AC theo từng story 2.1-2.7, realizes FR-6/7/9/10).

- Tất cả 7 story `done`, `pending_stories` rỗng — không có story dở dang chặn epic.
- Toàn bộ AC cấp story đã declared đều có bằng chứng test pass + code review verify trong chính story file; behavior check tại retro xác nhận build sạch + gần như toàn bộ test pass thật (217/218 test qua 2 project).
- Không có finding nào ở mức "blocking" (chặn nghiệm thu) — mọi finding trên đều đã hoặc có thể defer có kiểm soát, hoặc là process lesson không chặn chức năng.
- Có finding đáng kể còn mở, cần theo dõi tiếp (F1, F2, F4, F5) — đúng tiêu chí "accepted-with-open-items" thay vì "accepted" thuần.

## Open questions

- F5 (test-runner IPC error): cần anh CaoNN xác nhận phiên bản Node dùng ở môi trường deploy/CI thật để đánh giá đây có phải rủi ro sản xuất hay chỉ là quirk máy dev cụ thể.
- F1 (prune theo registry): cần quyết định hành vi mong muốn khi 1 kênh bị gỡ khỏi registry (publish 1 state "removed"? tự prune ngay khi reload? giữ nguyên tới khi restart?) trước khi dev thực hiện action item #1 — đây chính là câu hỏi đã bị hoãn từ review Story 2.2 (`deferred-work.md:69`), chưa từng được trả lời.
- Không xác nhận được liệu quyết định "giữ nguyên hành vi machine-offline đè subType REJECTED" (Story 2.7, F6 phần đối chứng tích cực) đã được anh CaoNN xác nhận trực tiếp hay chỉ do phiên code review tự ghi — nội dung story file khẳng định có ngày 2026-09-07 nhưng retro này không có log phiên riêng để đối chiếu độc lập.

## Assumptions

*(Chạy interactive, không phải headless — các mục dưới đã được xác nhận/quan sát trực tiếp trong phiên, không phải giả định không kiểm chứng, nhưng ghi lại để minh bạch quyết định phạm vi.)*

- Xác nhận với anh CaoNN trước khi vào Phase 1: không có concern riêng nào cần trọng tâm — retro chạy theo evidence chuẩn (xem câu hỏi mở đầu phiên).
- Diff range dùng empty-tree hash (`4b825dc6...`) làm điểm bắt đầu vì `52f312f` là commit gốc của repo, không có parent — thay cho `<first-commit>^` mà `git_evidence.py` giả định thường có.
- Story 2.2 không tự attribute được qua regex commit-subject của `git_evidence.py` (subject chỉ ghi "2.1-2.3", không khớp `\b2.2\b`) — đã đối chiếu thủ công qua nội dung story file/diff thay vì chỉ dựa vào script.
- Không chạy `bmad-review` như 1 subagent riêng cho toàn bộ diff 64 file — 7 story đã tự chạy `bmad-code-review` 4-layer nhiều vòng (đọc thấy trong chính story file), lặp lại toàn bộ sẽ trùng lặp phần lớn. Thay vào đó, retro này tự làm 1 lượt review hẹp, tập trung đúng các ranh giới liên-story (channelState.ts/wsUiAdapter.ts/main.ts — file bị chạm bởi nhiều story nhất) — đây là điểm thu hẹp phạm vi so với chỉ dẫn mặc định của skill, ghi lại tường minh ở đây.
