---
epic: 5
date: 2026-09-23
verdict: accepted-with-open-items
criteria: declared
headless: false
---

# Epic 5 Retrospective — Điều hướng bàn phím & khả năng tiếp cận đầy đủ (AA)

## Epic summary

- **Epic:** 5 — Điều hướng đầy đủ bằng bàn phím + Xác thực & hoàn thiện accessibility không phụ thuộc màu đơn lẻ (AA). Gate bắt buộc trước khi mở rộng pilot 1 kênh → 20 kênh thật.
- **Stories:** 5-1 (điều hướng bàn phím), 5-2 (accessibility AA) — cả 2 `done`, không có `pending_stories`.
- **Diff range:** `f368a76^..f78785b` (4 commit, 0 merge). Attribution theo subject commit (script `git_evidence.py`'s regex match không khớp id có dấu chấm `5.1`/`5.2` trong subject — ghi nhận gap công cụ, đã bù bằng đọc trực tiếp 4 subject commit): `f368a76` (feat 5.1), `5d69c52` (fix patch review round 2, 5.1), `5cf9beb` (feat 5.2), `f78785b` (fix patch review round 2, 5.2).
- **Files touched:** `ChannelGrid.tsx` (+22/-1), `ChannelGridCell.tsx` (+83/-19), `ChannelGridCell.module.css` (+120/-21), `DetailPanel.tsx` (+37/-0), `tokens.css` (+10/-0), cộng 3 file test (+458 dòng). Không file nào là god-class candidate mới (net churn cao nhất là test file, kỳ vọng bình thường); kích thước hiện tại của `ChannelGridCell.tsx` (409 dòng)/`.module.css` (431 dòng)/test (962 dòng) — lớn dần qua Epic 2/3/5 nhưng chưa tới mức cần tách file ngay, ghi nhận làm điểm theo dõi chứ không phải finding.
- **Evidence available:** epic spec (`epic-5-context.md`), 2 story spec (`spec-5-1-...md`, `spec-5-2-...md`, đầy đủ Review Findings + Completion Notes), sprint-status.yaml, retro epic 4 (liền trước). **Session logs: không có** — process-lesson analysis dựa hoàn toàn vào Review Findings/Completion Notes ghi trong story spec (đã khá chi tiết, có timestamp quyết định), không phải log hội thoại thật.

## Findings

3 lens `bmad-review` (adversarial, edge-case-hunter, verification-gap) chạy song song trên diff `f368a76^..f78785b`, mỗi lens độc lập không thấy kết quả của lens kia. Mọi finding dưới đây đã tự đối chiếu lại với source thật (không tin thẳng báo cáo sub-agent) trước khi routing.

### Lens findings — routed

**[Fix now] `.panel` (detail-panel dialog) không có rule `:focus-visible`** — *adversarial.* `DetailPanel.module.css:27` (`.panel {`) không có rule focus nào, trong khi `.ackInput`/`.ackButton` cùng file đã có `:focus-visible` dùng `--color-focus-ring` (dòng 209, 235), và `.cell:focus-visible` cũng vừa được Story 5.1 thêm cùng pattern. Đã verify bằng grep: chỉ 2 rule `:focus-visible` tồn tại trong file, không có `.panel`. Panel là phần tử nhận `.focus()` chương trình mỗi lần mở bằng bàn phím (dòng 211/158) — đúng chỗ Story 5.1 tự nhận cần có nhưng không làm; user tự defer 1 finding tương tự ở vòng review đầu ("`.panel` không có CSS `:focus-visible`... deferred, ngoài phạm vi AC") — nay có thêm góc nhìn: ngoài phạm vi AC hẹp, nhưng đúng tinh thần "gate rollout" của epic 5 (đảm bảo TOÀN dashboard dùng được bằng bàn phím) thì đây là dialog chính, không phải chi tiết phụ.

**[Fix now] `.cell:focus-visible` (viền focus của 20 ô kênh — chính là AC cốt lõi của Story 5.1) không có test nào khoá** — *verification-gap.* Grep toàn bộ `tests/` cho `focus-visible`/`outline`/`color-focus-ring`: 0 match. Story 5.2 đã tự thiết lập đúng pattern để khoá loại rule CSS này (source-text lock, `ChannelGridCell.test.tsx:567-584`, sau khi tự phát hiện lỗ hổng tương tự với `--color-cell-border`) nhưng không áp dụng ngược lại cho rule của 5.1. Xoá rule này, đổi `outline: none`, hay đổi màu sai AA — 276/276 test vẫn xanh.

**[Fix now] Trùng `gridPosition` → React key collision, không chỉ "thứ tự sai"** — *adversarial + edge-case-hunter, 2 lens độc lập cùng phát hiện.* `ChannelGrid.tsx:113`: `key={channel.gridPosition}` — verify trực tiếp source. Spec's frozen "Ask First" clause của Story 5.1 giả định hậu quả của gridPosition trùng là "thứ tự Tab sai", nên chỉ yêu cầu HALT hỏi người dùng nếu *thủ công* phát hiện dữ liệu thật bị trùng — nhưng cơ chế thật (React key trùng) không "sai thứ tự", nó **rớt hẳn 1 channel khỏi DOM** (React coi 2 phần tử cùng key là 1). Backend (`fileChannelRegistryAdapter.ts`) có enforce unique/0-19 nên đường phát sinh hiện tại chưa thấy, nhưng zero defense ở frontend — đúng kiểu "silent failure" mà phòng party mode đã cảnh báo trước khi vào retro này (dashboard báo bình thường, 1 ô kênh biến mất khỏi màn hình giám sát 24/7).

**[Fix now] Marker zone vu-meter (⚠/✕, tính năng cốt lõi Story 5.2) không có `aria-hidden` hay `aria-label`** — *adversarial + edge-case-hunter, 2 lens độc lập cùng phát hiện.* Verify: `ChannelGridCell.tsx:358-380`, span chứa `VU_METER_ZONE_MARKER_LABEL[zone]` (glyph trần) không có thuộc tính ARIA nào — khác các span khác cùng file đều có `aria-hidden="true"` (colorBars dòng 275, thumbnail dòng 286) hoặc có text đầy đủ ngữ cảnh (`ackLabel` dòng 403-405 "✓ Đã nhận: ..."). Cell cha có `aria-label={stationName}` đè lên, nên glyph trần vẫn lọt ra ngoài với AT duyệt theo ký tự — đúng người dùng mà epic này nhắm tới (không phụ thuộc màu) lại là người bị ảnh hưởng nhiều nhất.

**[Defer] `--color-cell-border` alias thẳng `--color-text-secondary`, không có test tính contrast thật** — *adversarial.* Verify: `tokens.css:26`. Rủi ro đã được chính người viết ghi chú ngay trong code (dòng 20-23: cảnh báo nếu `--color-text-secondary` bị retune sau này) — không phải gap ẩn hoàn toàn, nhưng test "khoá" (`ChannelGridCell.test.tsx:567-584`) chỉ match regex tên biến, không tính relative luminance thật — 1 lần retune token trong tương lai có thể phá AA của toàn bộ viền 20 ô mà không có test nào đỏ.

**[Defer] `.thresholdMark` (vạch ngưỡng cố định, tín hiệu phi-màu có từ trước) bị giảm bề rộng 6px→3px khi nhường chỗ cho marker mới, chưa re-validate độ nhận diện** — *adversarial.* Chưa verify bằng đo đạc thật (cần browser), ghi nhận là rủi ro hợp lý chứ không phải bằng chứng cứng.

**[Defer] Không có integration test grid+panel thật cùng lúc** — *adversarial.* Toàn bộ test focus-restore của Story 5.1 dùng `<button>` giả lập gắn thẳng vào `document.body`, không render `ChannelGrid` thật — đường nối thật giữa 2 mảng việc của story (cell thật → Enter/Space → panel → Esc → focus về đúng cell thật) chưa từng chạy trong test. Không cần dependency mới để làm (Never chỉ cấm `@testing-library/user-event`, không cấm render tích hợp).

**Không phải finding mới (đã có trong story spec từ trước, retro chỉ xác nhận lại):**
- Focus trả về cell đã MỞ panel lần đầu, không phải cell đang xem gần nhất khi chuyển kênh giữa lúc panel mở — story 5.1 tự ghi nhận đây là hành vi có chủ đích, đã defer từ vòng review trước (4 layer review độc lập cùng lúc đó cũng thấy, không có thông tin mới).
- `.ackLabel` vẫn dùng `--color-border` cũ (chưa đạt AA) — nằm trong Never của spec 5.2, đã ghi nhận tường minh ở `deferred-work.md`, không phải sai sót bỏ sót.

### Aggregate views (tự tính, không qua sub-agent)

- **Duplication map — TÁI DIỄN NHƯNG TỰ SỬA TRONG EPIC:** Story 5.2 review round 2 phát hiện `6px` (bề rộng cột vu-meter) lặp độc lập ở 3 nơi (`.vuMeterWrapper`/`.vuMeter`/`.vuMeterZoneMarker`) — cùng loại lỗi với `epic-2-retro-item-12` (hằng số grid lặp) — nhưng lần này bị code review của chính story bắt và gộp về biến `--vu-meter-width` **trước khi** đóng story, không để lại thành action item mới. [`spec-5-2-...md` Review Findings, patch cuối cùng; `ChannelGridCell.module.css:199-235,297-330`]
- **Spec-to-implementation reconciliation:** Không phát hiện lệch giữa `epic-5-context.md`/story spec và code — cả 2 AC của 5.1 và 3 AC của 5.2 đều có Completion Notes trích số đo/test cụ thể khớp acceptance criteria.
- **Nợ kỹ thuật liên-epic (item-10/11/12/19/20 từ epic 2/3):** Không file nào trong 4 file production bị đổi bởi epic 5 (`ChannelGrid.tsx`, `ChannelGridCell.tsx/.module.css`, `DetailPanel.tsx`, `tokens.css`) định nghĩa lại `DisplayState`, `GRID_SIZE`/`GRID_COLUMNS`/`GRID_POSITION_MIN/MAX`, hay thêm `Map` state mới (3 dòng `new Map()` trong diff đều là test fixture giả trong `ChannelGrid.test.tsx`, không phải state thật) — epic 5 **không làm nặng thêm** debt đã ghi nhận, nhưng cũng không trả debt đó (ngoài phạm vi AC của epic này).

## Behavior verification

**Không exercise runtime thật** — cả 2 story spec tự ghi nhận "Chưa xác nhận trực quan trên browser thật (không có môi trường browser lúc build)" (5.2 Completion Notes) và AC4 của 5.1 (Enter/Space trên nút ack) chỉ có manual-check checklist, không test tự động (quyết định đã xác nhận với người dùng 2026-09-23, do spec's Never cấm thêm `@testing-library/user-event`). Agent retro này cũng không có browser — **không có bằng chứng runtime nào mới**, chỉ kế thừa đúng giới hạn đã ghi trong story. Đây là gap thật cần người dùng tự làm (xem Open questions).

## Previous-retro follow-through

Nguồn: `action_items` trong `sprint-status.yaml`, 24 item `open` từ epic 1-4 (tính đến trước epic 5).

**Item epic 5's diff thực sự chạm tới / có bằng chứng mới:**

- `epic-1-retro-item-7` / `epic-2-retro-item-15` (Ask-First phải HALT thật, không phải tự quyết rồi xin xác nhận) — **open, không đổi.** Cả 2 "Ask First" clause đóng khung (frozen Boundaries) trong 5.1 (gridPosition trùng/ngoài 0-19) và 5.2 (không tìm được giá trị `--color-cell-border` đạt AA) đều **không bị trigger thật** — 5.1 tự xác minh backend đã enforce (không cần thử data thật để kích hoạt clause), 5.2 tìm được giá trị đạt AA ngay lần đầu (alias `--color-text-secondary`). Không có "Ask First" flag sống nào để kiểm chứng lại — giống hệt quan sát của `epic-4-retro-2026-09-23.md` dòng 81 cho epic 4. **Không phải bằng chứng "đã sửa"**, chỉ là "không có cơ hội tái phạm lần này".
  - Lưu ý khác biệt: 5.1 có 1 quyết định thật sự xảy ra giữa lúc code (AC4 test-coverage tradeoff) — nhưng đây **không phải** 1 trong 2 frozen "Ask First" clause của spec, mà là 1 finding phát sinh ở vòng review, được xác nhận với người dùng **sau khi code đã viết xong** (đúng lúc review, không phải HALT trước khi code). Không tính là vi phạm clause đóng khung (clause đó không liên quan AC4), nhưng cùng họ hành vi "resolve-sau-khi-code" mà `epic-1-retro-item-7` cảnh báo — ghi nhận làm quan sát định tính, không phải vi phạm mới.
- `epic-1-retro-item-8` / `epic-2-retro-item-16` / `epic-3-retro-item-21` (gate git-status-sạch trước khi set done) — **open, không đổi (cơ chế chưa xây)**, nhưng **hành vi sạch lần thứ 2 liên tiếp**: cả 4 commit epic 5 đúng khuôn 2-commit/story (feat + fix-patch-review-round-2), `git status` hiện tại sạch, không phát hiện patch uncommitted nào — cùng quan sát với epic 4 (`epic-4-retro-2026-09-23.md` dòng 82). Vẫn không có bằng chứng gate cơ chế đã được XÂY (item-21 đúng nghĩa vẫn mở), chỉ là kỷ luật thủ công đang giữ được 2 epic liền.
- `epic-3-retro-item-22` (đối chiếu status frontmatter story spec với sprint-status.yaml) — **kiểm tra: khớp.** `spec-5-1-...md`/`spec-5-2-...md` frontmatter `status: 'done'` khớp `sprint-status.yaml`'s `5-1-...: done`/`5-2-...: done`. Không tái diễn lệch như Story 3.1.

**Item không thuộc phạm vi diff epic 5 (frontend-only: `ChannelGrid.tsx`, `ChannelGridCell.tsx/.module.css`, `DetailPanel.tsx`, `tokens.css`) — không có bằng chứng mới theo chiều nào, vẫn `open`:**
`epic-2-retro-item-10` (Map prune), `-item-11` (DisplayState location), `-item-12` (hằng số grid), `-item-13` (node:test IPC), `-item-14` (checkpoint tích hợp WS thật), `epic-3-retro-item-17` (câu chữ spec-3-3), `-item-18` (test tích hợp reconnect/ack), `-item-19` (6 Map), `-item-20` (DisplayState+hằng số), `epic-4-retro-item-23` đến `-item-28` (toàn bộ thuộc alerting backend Telegram/email, ngoài phạm vi frontend của epic 5).

## Action items

**Remediation (fix-now, đề xuất — người dùng quyết định có mở story không):**

1. **[dev]** Thêm `.panel:focus-visible { outline: 2px solid var(--color-focus-ring); outline-offset: 2px; }` vào `DetailPanel.module.css`, mirror pattern `.cell`/`.ackInput`/`.ackButton` đã có — dialog chính của dashboard hiện không có viền focus themed khi nhận focus bằng bàn phím. [`DetailPanel.module.css:27`]
2. **[dev]** Thêm test khoá `.cell:focus-visible` (đọc source text `ChannelGridCell.module.css`, mirror đúng pattern Story 5.2 đã dùng cho `--color-cell-border` ở `ChannelGridCell.test.tsx:567-584`) — hiện AC cốt lõi của Story 5.1 (viền focus 20 ô) có thể bị xoá/đổi màu sai AA mà 276/276 test vẫn xanh.
3. **[dev]** Thêm dev-time validate/warning cho `gridPosition` trùng lặp ngay tại `ChannelGrid.tsx` (trước `.sort()`/trước khi dùng làm React `key`), hoặc đổi `key` sang `channel.channelId` (ổn định, không phụ thuộc tính hợp lệ của dữ liệu registry) — hiện `key={channel.gridPosition}` khiến 2 kênh trùng position không "sai thứ tự" mà **rớt hẳn 1 kênh khỏi màn hình giám sát 24/7**, không có defense nào ở frontend dù backend có enforce. Đây là mức độ nghiêm trọng cao nhất trong các finding của retro này — cùng họ "silent failure trong phòng trực" với `epic-1-retro-item-5` (RealSignal không forward frame). [`ChannelGrid.tsx:113`]
4. **[dev]** Thêm `aria-hidden="true"` cho marker zone vu-meter (`⚠`/`✕`) — mirror cách `colorBars`/`thumbnail` cùng file đã làm — glyph trần hiện lọt ra ngoài AT duyệt theo ký tự, đúng nhóm người dùng epic 5 nhắm tới bị ảnh hưởng nhiều nhất. [`ChannelGridCell.tsx:358-380`]

**Deferred (ghi nhận, không chặn, không cần quyết định ngay):**

5. `--color-cell-border` alias `--color-text-secondary` không có test tính contrast thật (chỉ regex khoá tên biến) — cân nhắc thêm 1 test tiện ích tính WCAG relative luminance thật thay vì chỉ khoá chuỗi. [`tokens.css:26`]
6. `.thresholdMark` giảm bề rộng 6px→3px khi nhường chỗ marker mới, chưa re-validate độ nhận diện trên màn hình thật — gộp chung nhóm "chưa xác nhận trực quan trên browser thật" đã ghi trong story.
7. Chưa có integration test grid+panel thật cùng lúc (test hiện dùng `<button>` giả lập thay vì render `ChannelGrid` thật) — không cần dependency mới để làm.

**Process lesson:**

8. **[process-owner]** `epic-1-retro-item-7`/`epic-2-retro-item-15` (Ask-First phải HALT thật) không có cơ hội tái kiểm ở epic 5 vì cả 2 frozen clause đều "không trigger" — nhưng chính vì không trigger nên KHÔNG AI validate được liệu guard đó có đủ hay không. Bằng chứng: finding #3 ở trên cho thấy chính giả định nền của Ask-First clause (gridPosition trùng → "sai thứ tự") đã sai — hậu quả thật là rớt channel. Đề xuất: khi 1 frozen "Ask First" clause không trigger, story phải tự hỏi thêm 1 bước "giả sử điều kiện NÀY xảy ra thật, có cơ chế nào (runtime hoặc test) bắt được không, hay chỉ đang dựa vào 'may mắn dữ liệu sạch'?" — biến Ask-First từ "chờ phát hiện thủ công" thành "có guard thật hoặc biết rõ mình không có". Đề xuất riêng: chuyển `epic-1-retro-item-8` (git-status sạch trước khi done) từ nhắc nhở sang gate cơ chế thật — epic 5 là epic thứ 2 liên tiếp giữ được kỷ luật thủ công (sau epic 4), đủ ổn định để đáng đầu tư 1 lần thành cơ chế thay vì tiếp tục hy vọng kỷ luật tái diễn ở epic 6.

## Acceptance verdict

**accepted-with-open-items** — cả 3 AC của Story 5.1 và 3 AC của Story 5.2 đều có bằng chứng đo đạc cụ thể (contrast đo bằng công thức WCAG, DOM order test, focus test), không có `pending_stories`, không phát hiện lệch spec-to-implementation nào. Verdict không hạ xuống "rejected" vì không finding nào mâu thuẫn trực tiếp với AC đã khai báo — 4 fix-now finding (panel focus-visible, test coverage của cell focus-visible, gridPosition key collision, marker aria-hidden) đều là gap **ngoài phạm vi hẹp của AC đã viết** (AC không yêu cầu panel có focus-visible, không yêu cầu test khoá CSS, không yêu cầu validate gridPosition runtime, không yêu cầu ARIA cho marker) nhưng **trong phạm vi tinh thần của epic** (gate rollout: toàn dashboard dùng được bằng bàn phím + không phụ thuộc màu đơn lẻ). "Open items" gồm cả 4 fix-now finding lẫn 2 gap tự khai từ trước (chưa xác nhận trực quan trên browser thật, chưa đo font/icon size trên TV wall thật) — tất cả cần đóng trước khi epic 5 thực sự thoả điều kiện "gate" để mở 20 kênh, dù không chặn việc set `status: done` cho epic 5 tại thời điểm này.

## Open questions

- TV-wall validation thật (kích thước chữ/icon ở khoảng cách xem thực tế) — `[ASSUMPTION cần xác nhận]` ghi trong `epic-5-context.md` dòng 23 — **chưa có bằng chứng nào cho thấy đã làm**. Đây là điều kiện tự epic 5 đặt ra trước khi mở 20 kênh, và không thể verify bằng code review — cần người dùng tự xác nhận trên phần cứng thật.
- Xác nhận trực quan trên browser thật (viền focus, marker zone, border contrast) — cả 2 story đều tự ghi "chưa làm, cần người dùng xác nhận".

## Assumptions

_(N/A — chạy interactive, không headless; các quyết định trong phiên này đều ghi trực tiếp ở Epic summary/Previous-retro follow-through)_
