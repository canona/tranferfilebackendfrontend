---
epic: 3
date: 2026-09-12
verdict: accepted-with-open-items
criteria: declared
headless: false
---

# Epic 3 Retrospective — Panel chi tiết kênh & xác nhận tiếp nhận cảnh báo (Ack)

## Epic summary

- **Epic:** 3 — dashboard-backend + dashboard-frontend, 3 stories (3.1 → 3.3).
- **`pending_stories` (qua `sprint_status.py detect-epic --epic 3`):** `3-1-ring-buffer-lịch-sử-bitrate-historyport-tri-state` — status `review` trong `sprint-status.yaml`, KHÔNG `done`. Đây chính là gate làm phiên chạy tương tác trước Phase 1: anh CaoNN đã xác nhận **tiếp tục retro** dù còn story này chưa đóng (xem "Assumptions" bên dưới) — nhưng mâu thuẫn cần nêu rõ: `spec-3-1-...md`'s frontmatter tự ghi `status: 'done'`, khác `sprint-status.yaml`. Đây là 1 desync bookkeeping (xem finding A6), không có bằng chứng code/test của 3.1 còn dang dở.
- **Đi vào retro:** không có concern riêng nào anh CaoNN nêu trước — chạy theo evidence chuẩn.
- **Diff range:** `b7387d0^..04e17ca` (`92ebed1..04e17ca`, loại trừ đúng commit CAP-5 dùng làm baseline). 6 commit, 0 merge, 27 file thay đổi (qua `git_evidence.py`).
  - Commit → story (đối chiếu thủ công, KHÔNG qua tự động — xem ghi chú công cụ dưới): `b7387d0`+`ee2288b` (3.1), `34349d0`+`3114011` (3.2), `b652faf`+`04e17ca` (3.3).
  - **Giới hạn công cụ:** `git_evidence.py`'s tham số `--stories 3-1,3-2,3-3` KHÔNG khớp được commit nào (`stories: []` cho cả 6 commit) — regex `\b3-1\b` không khớp text thật trong subject (`story-3.1`, dùng dấu chấm chứ không phải dấu gạch ngang). Đã đối chiếu thủ công qua nội dung subject/diff để gán đúng story cho từng commit; ghi lại đây để lần retro sau biết cần truyền `--stories 3.1,3.2,3.3` (khớp đúng ký tự trong message) thay vì id kiểu `sprint-status.yaml`.
  - Top churn (qua `files`): `DetailPanel.tsx` (+342/-37, 4 commit — component MỚI của 3.2), `wsUiAdapter.ts` (+327/-10, 2 commit), `channelState.test.ts`/`wsUiAdapter.test.ts`/`main.test.ts`/`channelStore.test.ts` (toàn bộ +200 đến +623 dòng — khối lượng test rất lớn so với code, đúng tinh thần nhiều vòng review/patch của cả 3 story).
- **Evidence có sẵn:** 3 story spec đầy đủ (Intent/Boundaries/Code Map/Review Findings/Spec Change Log rất chi tiết, 3.3 riêng có 4 vòng code review ghi lại), `epic-3-context.md`, `epics.md` (AC cấp epic, declared), `deferred-work.md` (262 dòng, xuyên suốt từ Epic 1), `sprint-status.yaml`, retro Epic 2 (`epic-2-retro-2026-09-07.md`). Codebase build+test chạy được thật tại thời điểm retro (xem Behavior verification).
- **Evidence thiếu:** Không có session log/conversation record nào cho từng story — giống Epic 1/2, process-lesson dựa vào "Spec Change Log" tự thuật trong story file, không phải log phiên làm việc độc lập.
- **Working tree tại thời điểm retro:** 4 file uncommitted (`dashboard-backend/src/adapters/outbound/wsUiAdapter.ts`, `dashboard-backend/tests/wsUiAdapter.test.ts`, `dashboard-frontend/src/state/channelStore.ts`, `dashboard-frontend/tests/channelStore.test.ts`) — nội dung khớp CHÍNH XÁC với "code review round 4" mà `spec-3-3-...md`'s Change Log (dòng 137-142) mô tả là đã hoàn tất + test pass. Xem finding A5 — đây là finding trung tâm của retro này.
- **Cập nhật trong lúc chạy retro (anh CaoNN đã xác nhận từng bước):** (1) đã commit 4 file trên — `f64dfeb` `fix(story-3.3): patch review round 4 (reset channelAck khi reconnect, sửa message log envelope_invalid)`; (2) đã sửa `sprint-status.yaml`'s `3-1` từ `review` sang `done` (khớp `spec-3-1.md`'s frontmatter, không có bằng chứng code/test nào còn thiếu — A6). Sau 2 thay đổi này, `pending_stories` cho epic 3 đã RỖNG (xác nhận lại qua `detect-epic --epic 3`) — verdict cuối của tài liệu này phản ánh trạng thái SAU 2 thay đổi đó, không phải trạng thái lúc bắt đầu phiên.

## Findings

Cả 3 story đã qua nhiều vòng code review 4-layer (3.1: 2 vòng, 3.2: 3 vòng, 3.3: 4 vòng) trước khi retro này chạy. Theo đúng tiền lệ Epic 2, **không invoke `bmad-review` như 1 subagent riêng cho toàn bộ diff** — thu hẹp phạm vi, tự chạy 1 lượt review (adversarial/edge-case/verification-gap) tập trung đúng: (a) ranh giới giữa 3 story (3.1→3.2→3.3 cùng chạm `channelState.ts`/`wsUiAdapter.ts`/`channelStore.ts`), và (b) chính khối uncommitted đang treo. Ghi lại tường minh đây là thu hẹp so với mặc định của skill.

### A5 — [fix-now, mức nghiêm trọng cao nhất] Patch "code review round 4" của Story 3.3 đã được ghi nhận HOÀN TẤT + TEST PASS trong spec file, nhưng KHÔNG có commit nào — toàn bộ nằm uncommitted

`spec-3-3-xác-nhận-tiếp-nhận-cảnh-báo-ack.md`'s Change Log (dòng 137-142, "code review round 4 — `/bmad-code-review`") mô tả 2 patch đã áp dụng + verify: (1) reset `channelAck` trong `applyRegistrySnapshot()` để sửa bug ack-label kẹt sau reconnect, (2) sửa message log `envelope_invalid`. Ghi rõ: "Backend 181/181 test pass sau patch... Frontend 221/221 test pass". Nhưng:

- `git log` không có commit nào sau `04e17ca` (round 3, `2026-09-12T12:21:52+07:00`) cho story 3.3.
- `git status` tại đầu phiên này cho thấy đúng 4 file (2 file nguồn + 2 test file) uncommitted, nội dung diff khớp CHÍNH XÁC 2 patch mô tả ở trên (đã đọc trực tiếp `git diff HEAD`).
- `sprint-status.yaml` đã `last_updated: 09-12-2026 23:00` — hơn 10 tiếng sau commit round-3 (12:21) — trùng khớp thời điểm round-4 lẽ ra phải xảy ra, và đánh dấu `3-3: done`, `epic-3-retrospective: optional`.

**Đã verify lại bằng cách chạy thật (không chỉ tin log tự thuật):** build+test backend/frontend với đúng working tree hiện tại (bao gồm cả patch uncommitted) — xem Behavior verification. Code CHẠY ĐÚNG, không phải regression giả — nhưng việc "done" trong `sprint-status.yaml` và nội dung "đã verify" trong spec file đang mô tả 1 trạng thái mã nguồn KHÔNG tồn tại trong git history. Nếu ai đó `git clean -fd`/`git checkout .`/chuyển branch mà không biết, patch này biến mất hoàn toàn không dấu vết, trong khi tài liệu dự án khẳng định nó đã "xong".

**Đây là lần tái diễn thứ 3** của đúng anti-pattern đã bị gắn cờ ở `epic-1-retro-item-8` và `epic-2-retro-item-16` ("git status sạch trước khi set done") — và là lần nghiêm trọng nhất: 2 lần trước, patch tồn đọng được tự commit lại trong CÙNG ngày trước khi bị phát hiện; lần này patch đã đứng yên đủ lâu để retro bắt đầu mà chưa commit, và tài liệu spec tự khẳng định "đã test pass" tạo cảm giác an toàn giả.

- **Nguồn:** `_bmad-output/implementation-artifacts/spec-3-3-xác-nhận-tiếp-nhận-cảnh-báo-ack.md:137-142`; `git log --oneline` (không có commit round 4); `git status --short`/`git diff HEAD` (đầu phiên); `sprint-status.yaml` dòng `last_updated`/`3-3: done`.
- **Kịch bản lỗi cụ thể:** Máy dev bị mất/ổ cứng hỏng trước khi ai đó commit, hoặc 1 lệnh `git reset --hard`/`git stash` vô tình — 2 bug fix thật (ack kẹt sau reconnect + log sai) biến mất, trong khi `sprint-status.yaml`/spec file vẫn khẳng định story "done" và đã verify — đội sau không có lý do gì để nghi ngờ lại.

### A6 — [reconciliation] `spec-3-1-...md`'s `status: 'done'` mâu thuẫn `sprint-status.yaml`'s `3-1: review`

Đây chính là nguyên nhân gate "unfinished story" kích hoạt đầu phiên retro này. Không có bằng chứng nào cho thấy 3.1 còn thiếu code/test (2 commit đã có, review 2 vòng đã xong, `deferred-work.md` chỉ còn 2 mục defer có lý do rõ) — nhiều khả năng đây thuần là quên cập nhật `sprint-status.yaml` từ `review` sang `done` sau khi review vòng 2 (`ee2288b`, 2026-09-12T07:16) đóng lại, khác hẳn A5 (code thật sự chưa commit).

- **Nguồn:** `spec-3-1-ring-buffer-lịch-sử-bitrate-historyport-tri-state.md:5`; `sprint-status.yaml`'s `development_status.epic-3.3-1-...`.
- **Disposition:** reconciliation đơn giản — cập nhật `sprint-status.yaml`'s `3-1` thành `done` (không phải code fix).

### A7 — [verification-gap, mới] Fix "ack-label kẹt sau reconnect" (round 4, xem A5) chỉ có test unit-level, thiếu test tích hợp WS thật cho đúng kịch bản reconnect

Test mới (`channelStore.test.ts`, trong khối uncommitted) gọi trực tiếp `store.applyAckChange(...)` rồi `store.applyRegistrySnapshot(...)` — xác nhận đúng hành vi CỦA STORE, nhưng không đi qua `uiWsClient.ts`/1 kết nối WS thật để mô phỏng đúng chuỗi sự kiện "client rớt kết nối → backend tự xoá ack → client reconnect → nhận registry-snapshot → nhận replay ack" như bug thật mô tả. Codebase ĐÃ có tiền lệ test tích hợp WS thật cho đúng loại hành vi "replay lúc connect" (`wsUiAdapter.test.ts`'s "client connect MUỘN... replay channel-ack-change", cùng file/cùng story) — nhưng test mới của round 4 không theo pattern đó ở phía frontend.

- **Nguồn:** `dashboard-frontend/tests/channelStore.test.ts` (diff uncommitted, test "applyRegistrySnapshot (mô phỏng reconnect) -> reset channelAck về rỗng"); đối chiếu `dashboard-backend/tests/wsUiAdapter.test.ts:1041+` (pattern tích hợp WS thật đã có).
- **Kịch bản lỗi cụ thể:** 1 thay đổi tương lai ở `uiWsClient.ts` (vd đổi thứ tự xử lý message, hoặc bug ở tầng parse khiến `registry-snapshot` không tới được `applyRegistrySnapshot`) sẽ không bị bắt bởi test hiện có — test chỉ khoá đúng 1 method của store, không khoá đường đi thật từ WS.

### A4 — [watch] `wsUiAdapter.ts` tiếp tục phình to, giờ giữ 4 cache replay + đã trở thành bidirectional

655 dòng — bị chạm bởi Story 2.3 (khởi tạo), 2.6, 2.7, 3.2, 3.3 liên tiếp. Giờ giữ `seenChannels`/`lastState`/`lastSnapshot`/`lastAckState` (4 cache riêng biệt phục vụ replay-on-connect) VÀ (mới từ 3.3) là điểm inbound-parsing DUY NHẤT nhận message từ client (`ack-command`) — dù tên file/port gốc (`UiOutboundPort`) hàm ý outbound thuần. Bản thân story 3.3 đã tự nhận đây là "ngoại lệ DUY NHẤT chiều ngược" và ghi lý do kiến trúc rõ ràng trong Design Notes — **không coi là finding mới**, nhưng xu hướng phình to (5 story liên tiếp cùng 1 file, 4 cache không prune — xem A1) đáng theo dõi cùng nhóm A1 khi quyết định tách file.

- **Nguồn:** `wc -l dashboard-backend/src/adapters/outbound/wsUiAdapter.ts` = 655; `spec-3-3-...md`'s Design Notes (dòng 146).
- **Disposition:** accept-as-is cho hiện tại (đã được lý giải tường minh), ghi chú theo dõi cùng A1.

### A1 — [fix-now, carry-forward, TỆ HƠN] `epic-2-retro-item-10` (Map không prune theo channel-registry) vẫn `open`, Epic 3 thêm 2 Map mới vào đúng nhóm lỗi

`BitrateHistoryService`'s `history` Map (3.1, `bitrateHistory.ts`) và `channelStore.ts`'s `channelHistory` Map (3.2) — cả 2 đều KHÔNG prune khi 1 channel_id bị gỡ khỏi registry, mirror đúng 4 Map đã bị gắn cờ ở Epic 2 (`channels`, `seenChannels`, `lastState`, `channelDisplayStates`/`channelMachineOffline`). Xác nhận qua grep trực tiếp: không có `.delete(`/prune nào liên quan registry-reload ở `bitrateHistory.ts`/`channelStore.ts`'s `channelHistory`. Tổng cộng giờ là **6 Map** cùng 1 root cause, action item Epic 2 (#1, `epic-2-retro-item-10`) vẫn `open`, chưa từng được thực thi dù đã tự nhận trong chính `deferred-work.md` của cả 3.1 và 3.2 là "nên gộp chung khi làm cơ chế dọn dẹp thống nhất, không phải fix riêng lẻ".

- **Nguồn:** `dashboard-backend/src/core/bitrateHistory.ts` (không có prune), `dashboard-frontend/src/state/channelStore.ts:58` (`channelHistory`), `deferred-work.md:221-222,239-240`, `sprint-status.yaml`'s `epic-2-retro-item-10` (`status: open`).
- **Kịch bản lỗi cụ thể:** giống hệt Epic 2 F1 nhưng quy mô rò rỉ bộ nhớ giờ lớn hơn (6 cấu trúc thay vì 4) — mỗi lần admin sửa channel-registry, dữ liệu tích luỹ ở nhiều nơi hơn.

### A2 — [fix-now, carry-forward, TỆ HƠN] `epic-2-retro-item-12` (hằng số lặp lại xuyên frontend/backend) vẫn `open`, Epic 3 thêm 2 cặp hằng số mới

`HISTORY_RETENTION_MS = 15*60*1000` định nghĩa độc lập ở `bitrateHistory.ts` (backend) VÀ `channelStore.ts` (frontend, dòng 58) — không cross-check test. `MAX_OPERATOR_LABEL_LENGTH = 64` định nghĩa độc lập ở `wsUiAdapter.ts` (backend, dòng 33) VÀ `DetailPanel.tsx` (frontend, dòng 26). Cùng nhóm root cause với `GRID_SIZE`/`GRID_COLUMNS`/`GRID_POSITION_MIN/MAX` đã gắn cờ ở Epic 2 (`epic-2-retro-item-12`, vẫn `open`) — action item đó chưa thực thi, Epic 3 tiếp tục thêm mẫu hình y hệt thay vì dừng lại giải quyết gốc.

- **Nguồn:** `dashboard-backend/src/core/bitrateHistory.ts:10`, `dashboard-frontend/src/state/channelStore.ts:58`, `dashboard-backend/src/adapters/outbound/wsUiAdapter.ts:33`, `dashboard-frontend/src/components/DetailPanel.tsx:26`, `deferred-work.md:242-243,260-261`.

### A3 — [fix-now, carry-forward] `epic-2-retro-item-11` (`DisplayState` định nghĩa trong file component, bị import ngược) vẫn `open`, không đổi trong Epic 3

Xác nhận qua grep: `DisplayState` vẫn định nghĩa tại `ChannelGridCell.tsx:36` (component UI), vẫn bị `channelStore.ts:16` import ngược (`import type { DisplayState } from '../components/ChannelGridCell'`). Epic 3 không chạm vào vị trí định nghĩa này dù có cơ hội (cả 3 story đều mở rộng `channelStore.ts`).

- **Nguồn:** `dashboard-frontend/src/components/ChannelGridCell.tsx:36`, `dashboard-frontend/src/state/channelStore.ts:16`.

### A8 — [tracked, không phải regression] `epic-2-retro-item-13` (lỗi `node:test` IPC ở `installService.test.ts`) tái hiện y hệt, vẫn được reference đúng

Chạy thật `cd dashboard-backend && npm run build && node --test "dist/tests/**/*.test.js"` (bao gồm cả patch round-4 uncommitted): **180/181 pass, 1 fail** — đúng `installService.test.js` với lỗi `Unable to deserialize cloned data due to invalid or unsupported version` y hệt Epic 2. Mỗi story 3.x đều tự ghi đúng "1 fail pre-existing, tracked epic-2-retro-item-13" trong Spec Change Log — **điểm tích cực**: dù chưa fix, việc reference nhất quán qua 3 story liên tiếp cho thấy kỷ luật ghi chú đang hoạt động tốt cho mục này, khác hẳn A5 (nơi 1 patch bị "quên" hoàn toàn khỏi git).

- **Nguồn:** chạy trực tiếp tại retro; `spec-3-3-...md:110,124,142` (mỗi lần đều reference đúng id).
- **Lưu ý phụ:** dòng 142 tự mâu thuẫn câu chữ ("Backend 181/181 test pass sau patch" rồi tự nói tiếp "1 fail pre-existing... tracked") — nitpick về cách diễn đạt log tự động, không phải finding riêng.

### P1 — [process lesson, trung lập] `epic-2-retro-item-15` (Ask-First phải HALT thật) — không có case nào để đối chiếu ở Epic 3

3.1 không khai báo Ask-First nào. 3.2 khai báo 2 mục Ask-First (đổi thư viện chart, đổi wire format) — cả 2 đều KHÔNG bị kích hoạt trong lúc code. Các "Decision needed" thật sự xuất hiện ở 3.2 (định danh phụ header, cho phép chuyển kênh khi panel mở) và 3.3 round 1 (thiếu cơ chế replay — 1 `bad_spec`) đều là phát hiện MỚI qua code review, không phải mục Ask-First đã khai báo trước bị vi phạm — đây là cơ chế khác (review → quyết định người dùng → patch), đúng quy trình, không phải tái diễn anti-pattern F6/item-15. Không đủ evidence để coi là "landed" (không có case thật để test), nhưng cũng không có vi phạm mới.

- **Nguồn:** `spec-3-1-...md:29`, `spec-3-2-...md:29-31,92-98`, `spec-3-3-...md:112-113`.

## Behavior verification

Đã build + chạy test thật trên đúng working tree tại thời điểm retro (bao gồm patch round-4 uncommitted, xem A5):

- `dashboard-backend`: `npm run build` sạch (tsc); `node --test "dist/tests/**/*.test.js"` → **180/181 pass** (1 fail = `installService.test.js`, xem A8, không liên quan epic 3).
- `dashboard-frontend`: `npm test` (vitest) → **221/221 pass** (8 file test); `npx tsc --noEmit` sạch.
- **Kết luận quan trọng cho A5:** code của round-4 (uncommitted) thực sự chạy đúng và test thực sự pass — vấn đề của A5 là kỷ luật commit/bookkeeping, KHÔNG phải chất lượng code.
- Không chạy manual E2E qua `npm run dev` thật (click thật trên trình duyệt) — hành vi runtime của luồng WS (ack round-trip, replay-on-connect, detail-panel 3 trạng thái) được xác nhận qua test tích hợp WS thật nội bộ từng story (đã đọc trực tiếp, đáng tin — cùng mức độ tin cậy áp dụng ở retro Epic 2, vì cần 20 máy trung tâm/hardware DeckLink thật để test đầu-cuối thật).

## Previous-retro follow-through (từ `epic-2-retro-2026-09-07.md`)

| id | Trạng thái file | Landed? | Đề xuất |
|---|---|---|---|
| `epic-2-retro-item-10-gộp-4-điểm-map-không-prune-theo-channel` | open | **Không landed — TỆ HƠN**: Epic 3 thêm 2 Map mới vào cùng nhóm lỗi (xem A1) | Không đổi trạng thái; giữ `open`, escalate ưu tiên |
| `epic-2-retro-item-11-dời-displaystate-hiện-định-nghĩa-trong-c` | open | Không landed, không đổi (xem A3) | Không đổi trạng thái |
| `epic-2-retro-item-12-gộp-hằng-số-kích-thước-lưới-grid_size-20` | open | **Không landed — TỆ HƠN**: Epic 3 thêm 2 cặp hằng số mới (xem A2) | Không đổi trạng thái; giữ `open`, escalate ưu tiên |
| `epic-2-retro-item-13-điều-tra-lỗi-node-test-ipc-unable-to-des` | open | Không landed nhưng tái xác nhận + reference đúng xuyên suốt (xem A8) | Không đổi trạng thái |
| `epic-2-retro-item-14-lên-lịch-1-checkpoint-tích-hợp-thật-giữa` | open | Không có bằng chứng — ngoài phạm vi diff Epic 3 (item thuộc architect, liên quan transport-core, repo khác) | Không đề xuất đổi |
| `epic-2-retro-item-15-tái-khẳng-định-epic-1-retro-item-7-ask-f` | open | Không có case nào để đối chiếu ở Epic 3 (xem P1) — trung lập | Không đề xuất đổi |
| `epic-2-retro-item-16-tái-khẳng-định-epic-1-retro-item-8-check` | open | **Tái diễn lần 3, nghiêm trọng hơn** (xem A5) | Không đổi trạng thái |

## Action items

**Fix-now (dev) — ưu tiên cao nhất trước:**
1. ~~**[dev]** COMMIT ngay 4 file uncommitted...~~ — **[done trong phiên retro này]** commit `f64dfeb`, anh CaoNN đã xác nhận. Còn lại: sửa câu chữ "181/181 test pass" trong Change Log (`spec-3-3-...md:142`) cho khớp thực tế (180/181, 1 fail pre-existing) — chưa làm, để lại cho dev.
2. **[dev]** Thêm 1 test tích hợp WS thật (mirror pattern `wsUiAdapter.test.ts`'s "client connect MUỘN... replay") cho đúng kịch bản reconnect làm mất ack-label (A7) — test hiện tại chỉ ở mức store-unit.
3. ~~**[dev]** Cập nhật `sprint-status.yaml`'s `3-1`...~~ — **[done trong phiên retro này]** đã đổi `review` → `done`, anh CaoNN đã xác nhận.
4. **[dev]** Thực thi thật `epic-2-retro-item-10` (gộp prune 6 Map, không phải 4 — xem A1) — đã bị hoãn qua trọn Epic 3 dù tự nhận nhiều lần cần gộp.
5. **[dev]** Thực thi thật `epic-2-retro-item-11` (dời `DisplayState`) và `epic-2-retro-item-12` (gộp hằng số lặp, giờ 3 cặp không phải 1 — xem A2/A3) — cùng lý do.

**Process lessons (process-owner):**
6. **[process-owner]** "Git status sạch trước khi set done" đã tái khẳng định 2 lần (Epic 1, Epic 2) mà vẫn tái diễn lần 3, nghiêm trọng hơn (A5) — đề xuất chuyển từ nhắc nhở sang **gate cơ chế**: bước đóng story của `bmad-build`/`bmad-dev-story` nên tự kiểm `git status --short` rỗng trước khi cho phép set `status: done`/cập nhật `sprint-status.yaml`, thay vì chỉ dựa vào tự giác.
7. **[process-owner]** Thêm bước đối chiếu `status` frontmatter của story spec với `sprint-status.yaml` trước khi tin bất kỳ nguồn nào là "đã done" (A6) — 2 nguồn lệch nhau không được phát hiện cho tới khi retro chạy.

**Spec reconciliation:** không có mục nào cần sửa `epics.md`/`epic-3-context.md` — as-built khớp đúng AC đã declared cho cả 3 story.

**Carry-forward không đổi (architect/pm):**
8. `epic-2-retro-item-14` — checkpoint tích hợp thật `TelemetryWsClient` (transport-core) ↔ `wsTelemetryAdapter` (dashboard-backend) — chưa có bằng chứng mới, vẫn cần trước khi pilot.

## Acceptance verdict

**accepted-with-open-items** (con người xác nhận — anh CaoNN — SAU khi 2 blocker dưới đây đã được xử lý trong chính phiên retro này).

- **Criteria:** declared (`epics.md`'s Epic 3 section + AC từng story 3.1-3.3, realizes FR-8/FR-12).
- **Diễn biến verdict trong phiên:** máy tính ban đầu là **rejected**, vì lúc bắt đầu Phase 1, `pending_stories` không rỗng (`3-1` = `review`, không `done` — hard rule của skill) VÀ A5 (patch round-4 của 3.3 chưa từng commit) đang treo. Anh CaoNN xác nhận tiếp tục retro trên epic dở dang đó, sau đó xác nhận cho xử lý cả 2 blocker ngay trong phiên: (1) commit `f64dfeb` cho patch round-4; (2) cập nhật `sprint-status.yaml`'s `3-1` → `done`. Chạy lại `detect-epic --epic 3` xác nhận `pending_stories` giờ RỖNG.
- Về CHẤT LƯỢNG: cả 3 story có bằng chứng test pass mạnh + nhiều vòng code review (2-4 vòng/story); behavior check tại retro (sau khi commit) xác nhận build sạch + 401/402 test pass thật qua 2 project (1 fail pre-existing `installService.test.ts`, không liên quan — A8). Không có finding nào chặn đúng-sai chức năng.
- Vẫn còn mở, cần theo dõi tiếp (đúng tiêu chí "accepted-with-open-items" thay vì "accepted" thuần): A1/A2/A3 (nợ kỹ thuật carry-forward từ Epic 2, epic 3 làm nặng thêm — Map-prune giờ 6 chỗ, hằng số lặp giờ 3 cặp, `DisplayState` layering chưa dời), A7 (verification-gap của chính patch vừa commit), A8 (lỗi test-runner pre-existing chưa điều tra).
- **Anh CaoNN đã xác nhận trực tiếp** verdict này (không phải override ngầm định) sau khi thấy rõ 2 blocker đã được xử lý — ghi theo đúng hard rule #1 của skill ("a human decision always overrides the machine verdict").

## Open questions

- A5: có cần audit lại các epic/story TRƯỚC đó xem có case tương tự nào (spec Change Log khẳng định "đã test pass" nhưng code chưa commit) bị bỏ sót không, ngoài 2 lần đã biết (Epic 1 item-8, Epic 2 item-16)?
- `epic-2-retro-item-10`/`11`/`12` đã bị hoãn qua 2 epic liên tiếp (Epic 2 → Epic 3) dù cùng được tự nhận "nên gộp" nhiều lần trong `deferred-work.md` — có cần chặn Epic 4 lại (không cho bắt đầu) cho tới khi ít nhất `item-10` (rủi ro rò rỉ bộ nhớ tích luỹ) được xử lý, hay tiếp tục defer?

## Assumptions

*(Chạy interactive, không phải headless.)*

- Đầu phiên, đã hỏi và anh CaoNN xác nhận **"Tiếp tục retro"** dù `pending_stories` không rỗng (3-1 = `review`) và working tree có 4 file uncommitted (nội dung hoá ra chính là A5) — quyết định này ghi nhận ở đây theo đúng yêu cầu Inputs section của skill.
- Story-attribution qua `git_evidence.py` thất bại hoàn toàn (0 commit nào khớp `--stories 3-1,3-2,3-3`) do lệch định dạng dấu gạch ngang/dấu chấm giữa id sprint-status và commit subject — đã tự đối chiếu thủ công thay thế, ghi rõ trong Epic summary.
- Không chạy `bmad-review` như 1 subagent riêng cho toàn bộ diff 27 file — 3 story đã tự chạy `bmad-code-review` 4-layer nhiều vòng; retro này tự làm 1 lượt review hẹp tập trung ranh giới liên-story + khối uncommitted, giống đúng cách tiếp cận đã ghi nhận ở retro Epic 2.
- Không chạy manual E2E qua trình duyệt thật (`npm run dev` + click tay) — dựa vào build sạch + test tích hợp WS thật đã có trong từng story, cùng mức độ tin cậy đã áp dụng ở Epic 2 (lý do: cần môi trường 20 máy trung tâm/hardware thật để test đầu-cuối, ngoài phạm vi máy dev hiện tại).
