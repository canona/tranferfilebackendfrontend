---
epic: 1
date: 2026-09-02
verdict: accepted-with-open-items
criteria: profiled
headless: false
---

# Epic 1 Retrospective — Truyền dẫn tín hiệu điểm-điểm ổn định qua SRT

## Epic summary

**Epic 1** (goal, in `epic-1-context.md`): xây đường truyền SDI→SRT→SDI tự chủ, điểm-điểm giữa mỗi đài và trung tâm, tự phục hồi (ABR + reconnect vô hạn + color bars), AES bắt buộc, tự thân thay thế đơn vị truyền dẫn thuê ngoài.

8/8 story `done` trong `sprint-status.yaml` (1.1–1.8). `pending_stories` từ `detect-epic --epic 1` rỗng — không có story dở dang, không cần xin xác nhận retro-trên-epic-chưa-xong. Chưa có retro nào chạy trước cho epic nào trong dự án này (không có previous-retro để đối chiếu follow-through).

**Diff range:** repo git thực tế nằm ở `transport-core/` (không phải root `TranferFiles/` — repo đó không phải git repo). Commit range `ca9b987..ef2d5bc` (11 commit non-merge, xem Phase 1 evidence bên dưới) — `ca9b987` là root commit (không có parent để dùng `^`, ghi nhận narrowing này). Cộng thêm: **11 file đang có thay đổi uncommitted trong working tree** trên top of `ef2d5bc` — xem Finding EPIC-1 bên dưới.

**Đánh giá theo tiêu chí:** epic không tự khai báo acceptance criteria ở cấp epic; verdict profiled từ AC của từng story + Goal/Requirements trong `epic-1-context.md`.

## Phase 1 — Evidence inventory

| Nguồn | Có sẵn? | Ghi chú |
|---|---|---|
| Epic spec / context | ✅ | `_bmad-output/implementation-artifacts/epic-1-context.md` (không có epics.md/PRD/architecture riêng trong `planning-artifacts/` ngoài `epics.md` — xem dưới) |
| Story files (8) | ✅ | 1 story file đầy đủ (1.1) + 7 `spec-1-X-*.md` (1.2–1.8), tất cả `status: done` |
| Sprint status | ✅ | `sprint-status.yaml`, epic-1 = `in-progress` (⚠ epic-level status KHÔNG khớp 8/8 story done — xem Finding EPIC-1) |
| Git history | ✅ (nhưng lệch project-root) | Repo thật ở `transport-core/`, không phải `TranferFiles/` gốc (không phải git repo). 11 commit, 1 branch riêng (`story-1-8-...`) chưa merge `main`, 11 file uncommitted |
| Diff range + per-story attribution | ✅ (1 gap) | `git_evidence.py` chạy trên `ca9b987..ef2d5bc`, stories `1.1..1.8`. 1 commit (`e9540d1`, "fix: harden provisioning...") không match story id nào trong subject dù nội dung khớp patch round-2 của spec-1-5 — attribution gap, xem Finding EPIC-2 |
| Previous retrospective | ❌ N/A | Chưa từng chạy retro nào trước — không có follow-through để đối chiếu |
| Session logs | ❌ Không có | Không tìm thấy file session/conversation log nào dưới `_bmad-output/`. Process-lesson analysis dựa hoàn toàn vào narrative rất chi tiết mà mỗi story tự ghi lại trong Completion Notes/Review Findings (đây là nguồn thay thế tốt, nhưng không phải session log gốc) — ghi nhận narrowing |
| `deferred-work.md` | ✅ | 2 mục epic-1 (từ review 1.7, 1.8), đọc đầy đủ |

## Phase 2 — Findings

### Aggregate views

**God-class growth — `ChannelActor.cpp`/`.h`:** file duy nhất bị TOÀN BỘ 7/8 story (1.2–1.8) sửa. Theo `git_evidence.py`: `ChannelActor.cpp` net +1290 dòng qua 8 commit (net-churn cao nhất toàn bộ diff), `ChannelActor.h` net +447 qua 5 commit. Kích thước hiện tại: **1658 dòng (.cpp) + 630 dòng (.h) = 2288 dòng** cho 1 class — chứa: connect/reconnect state machine (1.2), REJECTED routing+backoff riêng (1.4), playout-mode switching (1.3), source_type selection (1.6), ABR poll (1.7), telemetry/snapshot/heartbeat poll (1.8), toàn bộ trong `runStation()`/`runCenter()`. Mỗi story tự nó review sạch (3 layer/story), nhưng không session nào review `ChannelActor` như 1 khối — đây là hệ quả cấu trúc, không phải bug. [`transport-core/src/srt/ChannelActor.cpp`, `transport-core/src/srt/ChannelActor.h`]

**Spec-to-implementation reconciliation:**
- **Story 1.6 AC#2 (playout SDI thật) — chưa đạt, dù story mang status `done`.** `BlackmagicPlayoutSink::setMode(RealSignal)` chỉ ghi nhận trạng thái, KHÔNG forward frame decode thật từ `H264Decoder` ra card; kể cả `ColorBars` cũng chỉ ghi 1 frame tĩnh tại thời điểm chuyển mode, không có vòng lặp/clock đẩy frame liên tục sau đó. Tự ghi nhận trong Completion Notes của spec-1-6 là "khoảng trống chức năng thật sự (không chỉ thiếu test)". Người dùng đã quyết định (2026-09-02) chấp nhận trạng thái này và mở story mới để thiết kế đường truyền frame — quyết định human hợp lệ, nhưng nghĩa là **lời hứa cốt lõi của epic ("capture/playout SDI qua Blackmagic") chưa thực sự đạt được ở đầu ra** tại thời điểm epic đóng. [`transport-core/src/output/BlackmagicPlayoutSink.cpp:191-198`]
- **Toàn bộ nhánh hardware thật (Blackmagic capture/playout) chưa từng compile/chạy trên phần cứng thật.** `TRANSPORT_CORE_HAVE_BLACKMAGIC` được sửa để luôn compile (dùng FFmpeg vcpkg generic làm compile-check), nhưng FFmpeg custom decklink-enabled (`scripts/build-ffmpeg-decklink.ps1`) tự nhận "UNVERIFIED end-to-end" — chưa từng chạy vì thiếu Blackmagic SDK + MSYS2 trong mọi phiên làm việc đã qua. Không phải defect (đã document rõ, đúng "Ask First" flow) nhưng là rủi ro triển khai thật sự cho epic hứa "capture/playout SDI qua Blackmagic DeckLink Studio 4K". [`transport-core/scripts/build-ffmpeg-decklink.ps1`]
- **ABR (1.7) có thể bị đói đúng lúc kịch bản nó tồn tại để xử lý.** `sendAccessUnit()` block vô thời hạn khi buffer gửi SRT đầy (không có `SRTO_SNDTIMEO`) — đây CHÍNH LÀ tình huống "mạng xấu" mà ABR (Story 1.7) được viết ra để phản ứng trong ~1s. Deferred có chủ đích ("phạm vi lớn hơn 1 patch"), nhưng là gap thật giữa lời hứa NFR của epic ("ABR hạ bitrate... ưu tiên continuity") và hành vi thực tế dưới tải mạng xấu kéo dài. [`transport-core/src/srt/ChannelActor.cpp:711`, `transport-core/src/srt/SrtSocket.cpp:264`]
- **Heartbeat (1.8) không "độc lập connection_state hoàn toàn" như tên field/AC ngụ ý** — `listener.accept()` block vô hạn (libsrt 1.5.6 không hỗ trợ timeout cho `accept()`) khi chưa Station nào từng kết nối, chặn luôn vòng poll heartbeat/telemetry/snapshot cùng chỗ. Người dùng đã chấp nhận rủi ro cho MVP#1 pilot — ghi nhận, không phải defect ẩn. [`transport-core/src/srt/ChannelActor.cpp:1360-1372`]

### EPIC-1 — Round-2 code-review patches của Story 1.8 tồn tại CHỈ trong working tree, chưa từng commit

**Nguồn:** `git status`/`git diff --stat` (transport-core, nhánh `story-1-8-telemetry-vu-meter-snapshot-heartbeat`) đối chiếu với `### Review Findings` (không phải `## Spec Change Log`) của `spec-1-8-telemetry-...md`.

11 file đang modified, chưa `git add`/commit: `src/pipeline/H264Decoder.cpp/.h`, `src/source/BlackmagicSource.cpp/.h`, `src/srt/ChannelActor.cpp/.h`, `src/telemetry/SnapshotEncoder.cpp`, `src/telemetry/TelemetryWsClient.cpp/.h`, `tests/test_blackmagic_source.cpp`, `tests/test_telemetry_ws_client.cpp`.

Nội dung khớp CHÍNH XÁC 7 patch trong mục `### Review Findings` của spec-1-8 (round 2, tách biệt với 16+1 patch của `## Spec Change Log` round 1 — round 1 ĐÃ nằm trong commit `ef2d5bc`, verify qua `git show --stat ef2d5bc` khớp các file/dòng thêm mới): Design Notes ghi rủi ro heartbeat, gate `enableAudioMonitoring` cho `BlackmagicSource`, guard NaN cho `rtt_ms`, `try/catch` quanh `telemetryWsClient_->stop()`, 2 chỗ check return-value `sws_scale()`, log lỗi convert snapshot, test `StripControlCharsForHeader`. Story file ghi các mục này là **"ĐÃ ÁP DỤNG"** kèm `200/200 test pass` — nghĩa là công việc đã code+build+test thật, nhưng **kết quả đó chỉ tồn tại trên đĩa của máy hiện tại, không có trong bất kỳ commit/branch/remote nào.**

Cộng thêm 2 lớp lệch khác cùng chuỗi:
- Branch `story-1-8-telemetry-vu-meter-snapshot-heartbeat` (chứa cả 2 commit của story 1.8: `e9540d1`, `ef2d5bc`) **chưa merge vào `main`** — `main`/`origin/main` dừng ở `8e43fc4` (story 1.7). `origin/story-1-8-...` có 2 commit đó nhưng vẫn KHÔNG có 7 patch round-2 (chúng chưa hề được `git add`).
- `sprint-status.yaml` (`epic-1: in-progress`, không phải `done`) đã phản ánh đúng thực tế lệch này ở cấp epic — nhưng cả 8 story con vẫn ghi `done`, và `epic-1-context.md`/story files không có cảnh báo nào về việc code "done" của 1.8 chưa được chốt vào git.

**Vì sao quan trọng:** nếu working tree này bị mất (reset cứng, máy khác, clone mới), 7 patch đã build-verify (kể cả patch #8 sửa lỗi state "rách" thật trong `H264Decoder`, và patch guard NaN cho `rtt_ms`) biến mất hoàn toàn, không để lại dấu vết nào ngoài chính file spec — không ai biết phải làm lại patch nào nếu không đọc kỹ `### Review Findings` rồi tự tay áp lại. Đây là gap tích hợp thật, không phải lý thuyết: đúng lúc retro này chạy, `git status` xác nhận nó đang tồn tại.

[`transport-core` — `git status`, `git log --oneline --all --graph`, `_bmad-output/implementation-artifacts/spec-1-8-telemetry-thô-vu-meter-snapshot-heartbeat-qua-lan-có-xác-thực.md` mục `### Review Findings`]

### EPIC-2 — Commit `e9540d1` không gắn được với story nào qua subject, dù nội dung thuộc Story 1.5

**Nguồn:** `git_evidence.py` output (`"stories": []` cho commit `e9540d19b9`) đối chiếu nội dung message + `### Review Findings` của spec-1-5.

Commit `e9540d1` ("fix: harden provisioning input handling...") nằm giữa `40b982d` (story-1.7 round 2) và `ef2d5bc` (story-1.8) trên timeline, nhưng theo nội dung (BOM-strip, `ScopedConsoleEchoOff`, `weakly_canonical()`, `trimTrailingWhitespace()` — khớp *verbatim* các patch được liệt kê trong `### Review Findings` của spec-1-5) đây rõ ràng là patch round-2 cho **Story 1.5**, bị commit trễ (label commit tự nhận "Left uncommitted in the working tree from an earlier review pass"). Vì commit subject không chứa "1.5"/"1-5", công cụ đo bằng chứng tự động (`git_evidence.py`, và bất kỳ tool nào dựa vào subject để attribute) bỏ sót nó — chỉ phát hiện được qua đọc nội dung tay. Rủi ro thấp (nội dung đã trace được), nhưng là 1 lỗ hổng quy trình: **commit message không mang story id thì mọi phép đo tự động sau này (kể cả chính retro này nếu không đọc tay) đều lệch.**

[`transport-core` commit `e9540d1`, `_bmad-output/implementation-artifacts/spec-1-5-sinh-lưu-trữ-passphrase-an-toàn-qua-dpapi.md` mục `### Review Findings`]

### EPIC-3 — "Ask First" trong frozen Boundaries bị bỏ qua 2 lần liên tiếp ở Story 1.6 (process lesson, không phải code defect)

**Nguồn:** `spec-1-6-...md`, mục `### Review Findings` — 2 dòng `[Review][Dismissed] "Ask First #1"`/`"Ask First #3"`.

Story 1.6 có 2 mục "Ask First" trong frozen Boundaries: (1) đường dẫn SDK Blackmagic trước khi viết `build-ffmpeg-decklink.ps1`; (2) quyết định audio trong MPEG-TS mux trước khi đổi cấu trúc dữ liệu dùng chung. Cả 2 đều bị triển khai xong rồi mới tự biện minh trong Completion Notes, thay vì HALT hỏi TRƯỚC như spec yêu cầu literal — bị chính `bmad-code-review` (Acceptance Auditor) phát hiện ở round 3, không phải tự phát hiện. Người dùng xác nhận (2026-09-02) cả 2 quyết định kỹ thuật là hợp lý, không cần sửa lại — nhưng ghi nhận rõ trong spec: "cần quy trình HALT chặt hơn". *(Điểm này người dùng đã tự nêu ở đầu phiên retro này, khớp với memory đã ghi trước đó — [[bmad-build-ask-first-discipline]].)*

**Vì sao lặp lại đáng chú ý:** đây không phải 1 lần trượt cá biệt — 2/2 mục "Ask First" của CÙNG 1 story bị bỏ qua theo CÙNG 1 khuôn mẫu (tự quyết → ghi Completion Notes sau → chờ code review round 3 phát hiện thay vì tự HALT). Bằng chứng cho thấy vấn đề không phải "quên 1 lần" mà là khuôn mẫu hành vi của quy trình build khi gặp "Ask First": nó đọc như 1 gợi ý cân nhắc kỹ hơn trước khi tự quyết, không phải như 1 lệnh dừng cứng.

[`_bmad-output/implementation-artifacts/spec-1-6-capture-playout-sdi-qua-blackmagic-decklink-studio-4k.md` mục `### Review Findings`, dòng "Ask First #1"/"Ask First #3"]

### Diff-scope review (bmad-review, boundary files)

Chạy qua `bmad-review` (3 lens song song: adversarial, edge-case-hunter, verification-gap) trên diff của các file biên giới story so với root commit (`ca9b987`) — bao gồm cả 11 file uncommitted. Phạm vi narrowed có chủ đích xuống các file biên giới (`ChannelActor.cpp/.h`, `BlackmagicSource.cpp/.h`, `SrtSocket.cpp/.h`, `BlackmagicPlayoutSink.*`, `TelemetryWsClient.*`, `H264Decoder.*`) thay vì toàn bộ 14K dòng diff, vì mỗi story đã được review riêng 2-3 vòng — giá trị tăng thêm của lượt này nằm ở góc nhìn liên-story, không phải lặp lại review từng story.

**Verification-gap lens** — 2 finding, cả 2 đều rơi đúng vào phần code uncommitted (EPIC-1):
- `H264Decoder`'s toàn bộ nhánh lỗi convert-snapshot (dimension lẻ, `sws_getContext`/`sws_scale` fail) — nơi patch review round-2 của 1.8 thêm `snapshotConversionFailureCount()`/log-once — **0 test nào chạm tới**. Test snapshot duy nhất (`test_channel_actor_telemetry.cpp:296-349`) chỉ dùng frame 320×240 (chẵn), luôn đi nhánh thành công. `grep "snapshot_encode_error"` trên toàn bộ `tests/` = 0 kết quả. [`transport-core/src/pipeline/H264Decoder.cpp:97-178`, tiêu thụ tại `transport-core/src/srt/ChannelActor.cpp:696-703`]
- Nhánh Center tự mở `BlackmagicSource` thật để đo audio (patch #2 của round 1, gate theo `source_type=="blackmagic" && !dashboard_ws_url.empty()`) — **không có test nào dựng đúng tổ hợp điều kiện** để nhánh này thực sự chạy (test Center-blackmagic hiện có không set `dashboard_ws_url`; test có `dashboard_ws_url` thì `source_type` mặc định `"file"`). Nếu điều kiện gate bị đảo/lệch trong tương lai, 1 Center có card thật + dashboard cấu hình đúng sẽ âm thầm không bao giờ đo audio thật (`audio_level` luôn về sentinel) mà không test nào fail. [`transport-core/src/srt/ChannelActor.cpp:376-394,561-582`]

**Edge-case-hunter lens** — 5 finding, đáng chú ý nhất:
- **`sink_` (BlackmagicPlayoutSink, mở device làm OUTPUT) và `audioMonitorSource_` (BlackmagicSource, mở CÙNG `cfg_.blackmagic_device` làm INPUT) có thể tranh chấp cùng 1 device** khi `source_type=="blackmagic"` + `dashboard_ws_url` được set — cả 2 độc lập try/catch riêng, không có gì phát hiện/serialize việc 2 context cùng claim 1 thiết bị. Trên hardware không hỗ trợ duplex, 1 trong 2 sẽ âm thầm fail mà không có log nào nối 2 sự kiện lại với nhau. **Đây là bằng chứng độc lập, từ góc nhìn khác, cho đúng rủi ro kiến trúc mà chính spec-1-8 đã tự gắn cờ và người dùng đã chấp nhận rủi ro** ("thiết bị DeckLink có hỗ trợ mở đồng thời playout + capture-audio-only trên cùng 1 device index hay không" — chưa verify được, chờ hardware thật). [`transport-core/src/srt/ChannelActor.cpp` (`start()`, `sink_->open()` + audioMonitorSource_ construction cùng khoá theo `cfg_.blackmagic_device`)]
- `snapshotConversionFailureLogged_` chỉ reset ở `start()`, không reset mỗi khi `runCenter()` vào phiên `CONNECTED` mới (H264Decoder mới) — 1 phiên lỗi conversion thật ở phiên thứ 2 trở đi sẽ không bao giờ được log lại trong đời actor đó. [`ChannelActor.cpp` `sendSnapshot()`/`start()`]
- `runCenter()`'s `receiveAccessUnit()` không có timeout — peer treo giữa chừng (network partition không đóng socket hẳn) làm cả telemetry lẫn heartbeat ngưng tick, dù `state_` vẫn báo `CONNECTED` — cùng họ vấn đề với "Risk đã biết" của `accept()` blocking đã ghi nhận, nhưng ở 1 điểm khác (giữa phiên, không phải trước phiên đầu). [`ChannelActor.cpp` vòng nhận của `runCenter()`]
- `BlackmagicPlayoutSink::open()` không tự guard `fps_` (≤0/NaN/Inf) ở chính constructor — chỉ `ChannelConfig::validate()` chặn, nhưng constructor này reachable trực tiếp (test-only path) mà không qua validate. [`BlackmagicPlayoutSink.cpp` `open()`]
- `readFrame()` throw liên tục khi device báo kích thước lẻ bền bỉ → `runStation()`'s catch(...) log rồi continue KHÔNG throttle, khác mọi lớp lỗi khác trong cùng hàm (đều route qua RECONNECTING+backoff) — busy-loop log theo tần suất frame-rate thay vì backoff. [`BlackmagicSource.cpp` `readFrame()`, `ChannelActor.cpp` `runStation()`]

**Adversarial lens** — 11 finding, tuyển các mục nghiêm trọng nhất (đầy đủ 11 mục nằm trong transcript agent, có thể yêu cầu lại nếu cần):

1. **[Nghiêm trọng nhất]** `runStation()` reset `currentBackoff_`/`currentRejectBackoff_` về base NGAY khi `setState(CONNECTED)` — TRƯỚC khi khối setup source/encoder/mux (có thể fail) chạy xong. Nếu setup local (vd `BlackmagicSource::open()` trên build không có decklink thật — **chính là môi trường dev/CI hiện tại**) fail liên tục, actor đạt CONNECTED thoáng qua mỗi vòng → reset backoff mỗi lần → **busy-loop reconnect ở ~1s vĩnh viễn, không bao giờ leo thang tới trần 30s** — phá vỡ đúng thiết kế backoff cốt lõi của Story 1.2, đúng lúc tài nguyên cục bộ hỏng dai dẳng. [`ChannelActor.cpp` runStation(), quanh dòng setState(CONNECTED) và khối setup source/encoder/mux]
2. **[Nghiêm trọng]** `runCenter()`'s `H264Decoder decoder; decoder.open();` KHÔNG có try/catch bao quanh — bất đối xứng với `runStation()`'s khối setup (đã được vá try/catch tường minh ở review 1.6 đúng để tránh actor chết). Nếu `decoder.open()` throw, exception thoát ra `run()`'s outer catch → actor Center **kết thúc vĩnh viễn**, phá vỡ bất biến "reconnect vô hạn, không bao giờ chết hẳn" áp dụng nhất quán từ Story 1.2. [`ChannelActor.cpp` runCenter(), gần dòng khởi tạo `H264Decoder`]
3. Xác nhận CHÉO (độc lập với edge-case-hunter) tranh chấp thiết bị: `sink_` (output) và `audioMonitorSource_` (input) cùng mở `cfg_.blackmagic_device` đồng thời, không có gì phát hiện/nối lỗi của 2 bên lại với nhau khi 1 trong 2 fail — đúng rủi ro duplex mà `ChannelConfig.h`'s comment/spec-1-8 đã tự gắn cờ "chưa verify hardware thật".
4. **Sharpens finding AC#2 đã biết**: `RealSignal` mode không có bất kỳ `writeRealFrame()`-tương đương nào — sau khi chuyển `RealSignal` lần đầu, **KHÔNG BAO GIỜ ghi thêm packet nào ra DeckLink output nữa** trong suốt phiên CONNECTED đó. Trên hardware thật: SDI output đóng băng ở frame ColorBars tĩnh cuối cùng vĩnh viễn, trong khi `playoutMode()`/telemetry vẫn báo cáo `RealSignal` như đang phát sóng thật — trạng thái vận hành gây hiểu lầm, chỉ thấy được khi đối chiếu `BlackmagicPlayoutSink.h`'s scope note với điểm gọi `setMode()` ở `ChannelActor.cpp`.
5. `pumpForAudioLevelMetering()` (tới 64 lần `av_read_frame()` không timeout) chạy TRÊN CÙNG thread với `receiveAccessUnit()`/toàn bộ vòng nhận SRT của `runCenter()` — nếu audio-monitor device treo, CẢ vòng nhận SRT/decode/telemetry của actor bị chặn vô hạn theo, không chỉ audio — cùng họ rủi ro "blocking call không timeout" với `accept()` (đã ghi Risk đã biết) và `sendAccessUnit()` (đã defer ở 1.7), nhưng ở 1 điểm THỨ BA chưa từng được ghi nhận trong bất kỳ story nào.
6. `rtt_ms` clamp cả "lỗi đọc thật" LẪN sentinel hợp lệ "-1 = chưa có mẫu RTT" của libsrt về CÙNG giá trị 0.0 — dashboard (Epic 2) không phân biệt được "vừa connect, chưa có mẫu" với "RTT thực sự bằng 0ms".
7. `BlackmagicSource::open()` bắt buộc tìm được video stream ngay cả khi instance chỉ dùng để audio-monitor (`enableAudioMonitoring=true`, không bao giờ gọi `readFrame()`) — 1 device chỉ có audio embedded hợp lệ (không có video decode được) sẽ làm audio-monitoring fail hoàn toàn dù tính năng cần không phụ thuộc video.
8. `setPlayoutSinkForTesting()` thiếu null-check mà hook song sinh `setSourceFactoryForTesting()` đã được vá thêm ở review trước — lệch kỷ luật giữa 2 test hook cùng vai trò.

*(3 finding còn lại — duplicate `ensureAvdeviceRegistered()` guard giữa 2 file, `sink_` dựng vô điều kiện trong ctor kể cả role Station, `event_type="source_error"` gộp lẫn lỗi mux với lỗi capture — mức độ thấp hơn, ghi nhận nhưng không đưa vào action item riêng.)*

### Behavior verification

Retro này tự chạy lại build+test thật trên working tree hiện tại (build dir đã configure sẵn từ phiên trước, toolchain `D:\VSBuildTools2`):
- `cmake --build build --config RelWithDebInfo` → `ninja: no work to do` (object đã build sẵn từ phiên trước, khớp đúng working tree hiện tại — xác nhận không có gì chưa build).
- `ctest --test-dir build --output-on-failure -C RelWithDebInfo` → **201/201 test pass** (123.5s), gồm cả 7 patch uncommitted (EPIC-1). **Xác nhận độc lập: code trong working tree — kể cả phần chưa commit — build sạch và test pass thật, đúng như story 1.8 tự báo cáo.** Đây là tin tốt về CHẤT LƯỢNG code; vấn đề ở EPIC-1 thuần túy là nó chưa được `git add`/commit, không phải nghi ngờ về tính đúng đắn của patch.

Không tái tạo được phần cần hardware Blackmagic/mạng WAN thật (không có card/SDK/throttle trong môi trường retro) — giữ nguyên trạng thái "chỉ trace logic" mà các story đã tự ghi nhận cho AC#1/AC#2 của Story 1.6 và phần audio-monitor thật của Story 1.8.

## Previous-retro follow-through

N/A — đây là retro đầu tiên của dự án (không có `action_items` nào trong `sprint-status.yaml` thuộc epic trước).

## Phase 4 — Action items

| # | Hạng mục | Disposition | Nguồn | Owner đề xuất |
|---|---|---|---|---|
| 1 | **Commit + merge 7 patch round-2 của Story 1.8** đang nằm uncommitted trong working tree (`H264Decoder`, `BlackmagicSource`, `ChannelActor`, `SnapshotEncoder`, `TelemetryWsClient` + 2 test file), rồi merge branch `story-1-8-telemetry-...` vào `main`. Đã reverify 201/201 pass — chỉ còn thao tác git. | Fix now (khẩn — chặn trước khi Epic 2 bắt đầu, vì Epic 2/dashboard-backend phụ thuộc trực tiếp contract telemetry của 1.8) | Finding EPIC-1 | Dev (bất kỳ ai chạm `transport-core` phiên tới) |
| 2 | **Sửa thứ tự reset backoff trong `runStation()`**: chỉ reset `currentBackoff_`/`currentRejectBackoff_` SAU KHI khối setup source/encoder/mux thành công, không phải ngay sau `setState(CONNECTED)` — tránh busy-loop reconnect ~1s vĩnh viễn khi setup cục bộ hỏng dai dẳng (đúng kịch bản môi trường dev/CI hiện tại không có FFmpeg-decklink thật). Thêm regression test. | Fix now | Adversarial lens #1 | Dev |
| 3 | **Bọc try/catch quanh `H264Decoder decoder; decoder.open();` trong `runCenter()`**, route lỗi qua RECONNECTING như mọi lỗi khác trong hàm — hiện exception thoát thẳng ra `run()`'s outer catch, kết thúc actor Center vĩnh viễn, phá bất biến "reconnect vô hạn" của epic. | Fix now | Adversarial lens #2 | Dev |
| 4 | **Verify/guard xung đột thiết bị duplex** (`sink_` mở OUTPUT + `audioMonitorSource_` mở INPUT cùng `cfg_.blackmagic_device`) trên hardware DeckLink thật — hoặc thêm field cấu hình riêng cho device audio-monitor nếu duplex không được hỗ trợ. Xác nhận độc lập 3 lần (spec-1-8 tự gắn cờ, edge-case-hunter, adversarial). | Fix now (cần hardware để verify — không tự làm được trong sandbox) | spec-1-8 Completion Notes + edge-case-hunter + adversarial | Dev + Ops (cần quyền truy cập DeckLink) |
| 5 | **Story mới: forward frame decode thật vào `PlayoutSink`** (Story 1.6 AC#2) — `RealSignal` mode hiện không bao giờ ghi thêm packet nào ra card sau lần chuyển mode đầu tiên; SDI output sẽ đóng băng ở color-bars tĩnh trong khi telemetry báo `RealSignal`. Người dùng đã quyết định mở story mới (2026-09-02); retro này khẳng định lại đây là điều kiện bắt buộc trước khi có thể nói "SDI output thật khớp tín hiệu đài" trong bất kỳ tài liệu vận hành nào. | Defer (đã có quyết định human, tracked) | spec-1-6 Completion Notes + Adversarial lens #4 | Product/PM (mở story) |
| 6 | **Gộp 3 điểm blocking-call-không-timeout thành 1 story riêng** thay vì vá rời rạc: `sendAccessUnit()` (ABR đói, đã defer ở 1.7), `listener.accept()` (heartbeat, đã accept risk ở 1.8), `pumpForAudioLevelMetering()`'s `av_read_frame()` (mới phát hiện ở retro này — có thể treo TOÀN BỘ thread `runCenter()`, không chỉ audio). Cả 3 cùng 1 nguyên nhân gốc: libsrt 1.5.6/FFmpeg blocking API không hỗ trợ timeout trong vòng lặp actor. | Defer, đề xuất gộp thành 1 spec | 1.7 deferred-work.md + spec-1-8 Design Notes + Adversarial lens #5 | Dev (story riêng, ước lượng effort khi lập kế hoạch) |
| 7 | **Quy trình "Ask First" trong frozen Boundaries phải HALT thật, không phải lời nhắc cân nhắc kỹ hơn.** 2/2 mục Ask First của Story 1.6 bị bmad-build tự triển khai xong rồi mới giải trình trong Completion Notes — bị chính code review (không phải tự nhận ra) phát hiện ở round 3. Người dùng xác nhận quyết định kỹ thuật đúng cả 2 lần, nhưng đây là lần thứ 2 mẫu hình này lặp lại TRONG CÙNG 1 story — dấu hiệu quy trình, không phải sự cố cá biệt. | Process lesson | spec-1-6 Review Findings + memory `bmad-build-ask-first-discipline` | Người bảo trì skill/prompt `bmad-build` (hoặc dev-agent instructions) |
| 8 | **Kỷ luật commit trước khi đóng story `done`.** 2 dấu hiệu trong cùng epic: (a) `e9540d1` — patch round-2 thật của Story 1.5 bị commit dưới message không mang story id, khiến công cụ đo bằng chứng tự động bỏ sót; (b) Finding EPIC-1 — story 1.8 đóng `done` trong khi 7 patch đã build-verify vẫn nằm uncommitted. Đề xuất: checklist "đóng story" của `bmad-build`/dev-agent thêm bước xác nhận `git status` sạch (mọi thay đổi đã commit, message mang đúng story id) trước khi set `status: done`. | Process lesson | Finding EPIC-1, EPIC-2 | Người bảo trì skill/prompt `bmad-build` |
| 9 | **Dọn nhóm finding mức thấp** khi ai đó quay lại làm item #4/#5 ở trên (không cần patch riêng ngay): `rtt_ms` clamp lẫn lộn "chưa có mẫu" với "0ms thật"; `BlackmagicSource::open()` đòi hỏi video stream ngay cả khi chỉ audio-monitor; `setPlayoutSinkForTesting()` thiếu null-check (đối xứng với `setSourceFactoryForTesting()` đã có); `snapshotConversionFailureLogged_` không reset mỗi phiên CONNECTED mới; 2 gap test coverage cho chính 7 patch uncommitted (nhánh lỗi convert-snapshot, nhánh gate audio-monitor thật) — verification-gap lens. | Defer, bundle | Adversarial #3,#6,#7,#8 + Edge-case-hunter + Verification-gap | Dev |

## Phase 4 — Acceptance verdict

**accepted-with-open-items**

**Căn cứ:**
- Tiêu chí profiled từ AC của 8 story (không có acceptance criteria khai báo ở cấp epic) + Goal/Requirements trong `epic-1-context.md`. `pending_stories` rỗng — không có story dở dang buộc verdict `rejected`.
- **Đạt:** 7/8 story đạt đầy đủ AC của chính nó, có test thật + build thật verify (retro này tự chạy lại độc lập, không chỉ tin lời story: 201/201 test pass trên working tree hiện tại, kể cả phần uncommitted). Kiến trúc actor-per-channel, AES bắt buộc, reconnect/REJECTED/color-bars/ABR/telemetry đều có code + test thật, không phải khung sườn rỗng.
- **Chưa đạt / mở:**
  - Story 1.6 AC#2 (playout SDI thật) — GAP đã biết, có quyết định human (mở story mới), không phải điều bất ngờ.
  - **2 defect mới, chưa từng được 3 vòng code review nào của bất kỳ story nào phát hiện** (Adversarial #1, #2) — cả 2 đều đụng trực tiếp vào bất biến cốt lõi của epic ("reconnect vô hạn, không bao giờ chết hẳn"; "backoff leo thang chống thundering"). Đây là bằng chứng sống cho đúng lý do retro tồn tại: lỗi chỉ hiện ra khi nhìn `ChannelActor.cpp` như 1 khối, không phải qua từng diff story riêng lẻ.
  - **Finding EPIC-1**: phần code mới nhất (patch round-2 của 1.8) chưa hề được commit — về mặt git, "epic 1 done" chỉ đúng tới `main`@story-1.7; `sprint-status.yaml` tự nó đã ghi `epic-1: in-progress` (không phải `done`), khớp đúng thực tế này.
  - Rủi ro duplex hardware (đầu Center) và nhóm blocking-call-không-timeout — đã biết/deferred có chủ đích, chưa verify được trên hardware thật.
- Không mục nào trong các mục "chưa đạt" ở trên đủ để coi là *blocking* theo nghĩa "epic không hoạt động được" — code build sạch, test pass, luồng end-to-end (loopback) chạy đúng. Nhưng cũng không đủ sạch để `accepted` không điều kiện: 2 defect mới + gap commit là named, cụ thể, cần theo dõi tới khi đóng.
- **Khuyến nghị mạnh (không phải điều kiện cứng của verdict, nhưng nên cân nhắc trước khi mở Epic 2):** action item #1 (commit+merge) nên xong TRƯỚC KHI Epic 2 bắt đầu, vì Epic 2 (dashboard-backend) phụ thuộc trực tiếp vào đúng contract telemetry mà 1.8's patch round-2 sửa (guard NaN cho `rtt_ms`, log lỗi snapshot) — bắt đầu Epic 2 trên 1 baseline chưa merge tăng rủi ro conflict/mất việc đã làm.

## Open questions

- Rủi ro duplex device (finding #4) có thật trên hardware DeckLink Studio 4K cụ thể của dự án hay không — chỉ verify được khi có hardware thật, chưa ai xác nhận.
- `scripts/build-ffmpeg-decklink.ps1` (build FFmpeg custom decklink-enabled) chưa từng chạy end-to-end — cho tới khi ai đó chạy nó với SDK/MSYS2 thật, không biết liệu Story 1.6's nhánh capture/playout thật có compile được hay không, ngoài code review tĩnh.
- Item #6 (gộp 3 điểm blocking thành 1 story) — chưa ước lượng độ lớn công việc thật (chuyển sang SRT epoll non-blocking là thay đổi kiến trúc vừa phải, không phải patch nhỏ).

## Assumptions

*(Chỉ áp dụng cho run headless — run này chạy interactive, epic được user xác nhận ở đầu phiên, không có giả định nào cần ghi ở đây ngoài mục đã nêu trong Epic summary.)*
